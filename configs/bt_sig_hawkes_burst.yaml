# APP research backtest — single-alpha config for sig_hawkes_burst_v1.
#
# Usage:
#   uv run python scripts/run_backtest.py --config configs/bt_sig_hawkes_burst.yaml \
#       --symbol APP --date 2026-03-26
#
# Inherits platform defaults via ``extends:``; only APP-specific deltas below.
extends: ../platform.yaml

symbols:
  - APP

alpha_specs:
  - alphas/sig_hawkes_burst_v1/sig_hawkes_burst_v1.alpha.yaml

signal_min_edge_cost_ratio: 1.5

parameter_overrides: {}
