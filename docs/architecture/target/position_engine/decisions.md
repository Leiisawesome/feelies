# Decisions

Append-only. Decisions the spec did not cover, taken during the build. Each entry: id, date,
rung, the question, the decision, the reason (no reference to results), and whether the
spec was amended upstream. A decision that should have been in the spec gets a matching
edit to the spec file in a later docs rung, citing the entry.

| Id | Date | Rung | Question | Decision | Reason | Spec amended |
|---|---|---|---|---|---|---|
| D-01 | 2026-09-23 | P-10 | How are position moves carried? | moves carried as exact integer cents over the whole position (entry is an exact rational of partial fills; per-share ticks are not exact; thresholds compare total >= X*size*tick) | | pending docs rung |
| D-02 | 2026-09-23 | P-10 | What is the cell's fill source? | SlicePositionUpdate is the cell's fill source, own stream slice_position (PositionUpdate is symbol-net without strategy or fill detail; own stream keeps orchestrator numbering intact) | | pending docs rung |
| D-03 | 2026-09-23 | P-10 | How is the position engine enabled? | enable via build_platform(enable_position_engine=) until P-15 exit_policy; non-BACKTEST raises (no config field, so the config hash cannot move) | | pending docs rung |
| D-04 | 2026-09-24 | P-10 | Do the architecture gates apply to the new engine? | new engine's forbidden-reads rows, subscriber-engine pin 7->8, audit owner, and the mode guard placed in the mode seam | the architecture's own gates apply to a new engine; the guard moved rather than widening the seam test | pending docs rung |
| D-05 | 2026-09-24 | P-10 | What checks the position SUBSCRIPTIONS rows when the engine is off? | S15 builds with the engine off, so the position SUBSCRIPTIONS rows were verified by nothing | T7 runs the runtime-subset check on an enabled build | pending docs rung |
| D-06 | 2026-09-24 | P-10 | S14's dynamic probe builds the default platform, so a dark engine is never observed | the S14 replay builds with enable_position_engine=True; a negative probe proves it catches an engine-13 forbidden read | | pending docs rung |
| D-07 | 2026-09-24 | P-11a | D-01..D-06 recorded "pending docs rung" | spec amended: contracts.md (wiring, SlicePositionUpdate, cents moves and comparisons, enabling, mode seam, structural checks), battery.md (member 4 in cents, structural checks, stage B), amendments.md A-16/A-17, phase14 plan (P-10 done, P-11a, P-30 rescoped, parity facts, capture rule) | an append-only log is closed by a new row, not by rewriting old ones | yes |
| D-08 | 2026-09-24 | P-15 | schema version bump for exit_policy? | no bump; opt-in block by presence (P15-a) | a bump rewrites 11 alpha files, moving the config snapshot and _BASELINE_CONFIG_HASH for no semantic change; precedent hazard_exit, safety_exit_policy | yes (SCHEMA.md) |
| D-09 | 2026-09-24 | P-15 | loader ignored unknown top-level keys | closed set = SCHEMA tables + universe, factor_neutralization, safety_exit_policy, notes + exit_policy; table rewritten to list all (amended P15-b) | a mistyped exit_policy silently leaves positions unmanaged; the measured key union showed the tables incomplete, not the alphas wrong | yes (SCHEMA.md) |
| D-10 | 2026-09-24 | P-15 | L6 vs session flatten on by default | stop/trail thresholds must be 0; session flatten allowed only with buffer < every exit_policy cutoff (P15-c) | the engine's deadline exits first, so two authors cannot both flatten an owned slice | pending docs rung (battery.md L6) |
| D-11 | 2026-09-24 | P-15 | gate alias name for the exit_policy checks | "EXIT_POLICY" -> GOV.LAYER_VALIDATE, no GATE_REGISTRY row | G-numbers are gap ids (G41–G46 exist); no test pins alias keys or size | pending docs rung |
| D-12 | 2026-09-24 | P-15 | test helper _platform() lacked the fixture alpha's sensors, so E11/E12 could not reach their checks | amendment A-P15-1: _platform() reuses configs/bt_netting_contest.yaml:32 (PlatformConfig.from_yaml); fail-first re-run shows E11/E12 failing for the intended reason | a test that cannot reach what it names is wrong; fixing it needs no reference to results | n/a |
| D-13 | 2026-09-24 | P-15 | six top-level keys outside the amended set (author, regimes, construct, data_sources, fill_model, story_permission) were needed by existing load tests; the key-union census scanned YAML files only and missed inline test specs | kept in the closed set, each with a SCHEMA.md row; rule stated precisely: a top-level key is in the closed set if production code reads it or it is documented as an annotation with no runtime meaning. Five are read in production (loader.py:412, loader.py:563, layer_validator.py:358 G14, :387 G15, :1322 G17); author is an annotation, same class as notes | a census inherits the blind spots of what it scans; dropping a production-read key would silently break real alphas | yes (SCHEMA.md) |
| D-14 | 2026-09-24 | P-16 | What does parity protect once the engine can change results? | Three sources of evidence: the legacy oracle (frozen), the battery (correctness), and a position oracle pinned at P-99 (engine-on regression). | Legacy parity proves the control is unchanged; it cannot guard the engine-on path, and the battery does not run on real data. | evaluation.md §1 |
| D-15 | 2026-09-24 | P-16 | What makes a declared break acceptable? | Pre-registration: predicted constants, direction, fills and mechanism are stated before the pre-capture; unpredicted or contradicted moves block. | Without a written prediction the only remaining test is whether numbers improved, which turns the oracle into a fitting target. | evaluation.md §2 |
| D-16 | 2026-09-24 | P-16 | How are engine-on differences on real data reviewed? | P-95 attribution diff: episode-matched, classed, descriptive only. | Campaign 15 must explain every changed trade before it can be reviewed. | evaluation.md §3; plan ladder |
| D-17 | 2026-09-24 | P-16 | Where is "better" established? | Campaign 14E (calibration, out-of-sample paired evaluation, pre-registered bar) gates campaign 15. | Exit rules cannot create edge on a driftless price; improvement is a risk-adjusted, out-of-sample, cost-netted claim that a single day cannot carry. | evaluation.md §4; plan |
| D-18 | 2026-09-24 | P-16 | How do blocking members land red with CI green, without build rungs editing tests? | A stage letter in stage.txt; members declare GREEN_FROM and RED_REASON; a conftest hook enforces them. | xfail would require editing the test when the member turns green. | battery.md |
| D-19 | 2026-09-24 | P-16 | The fixture alpha is data-gated and stamps sig_contra_fixture_v1. | P-21 makes entry deterministic by schedule and stamps sig_position_fixture_v1. | The members need known entries; a wrong strategy_id assigns slice ownership to the wrong alpha. | battery.md |
| D-20 | 2026-09-24 | P-16 | Where do real-session members run? | Marker battery_real, collected by the parity oracle job with the cache required. | The check job excludes the APP cache, so those members would otherwise be skipped silently. | battery.md; evaluation.md §5 |
| D-21 | 2026-09-24 | P-11 | How does the book get the rail's valuation rule while the rail is dark? | One pure classifier in core/quote_quality.py, used by the book now and the rail at P-40. | The book always runs; the rail runs only when the engine is on. | contracts.md book mark; A-18 |
| D-22 | 2026-09-24 | P-11 | What happens to the stored mid? | Retired as a valuation input; kept as reference_mid (last VALID quote) for sizing and exposure notional; latest_mark is an alias. | Valuation must be executable-side; sizing and exposure measure scale, not P&L. | contracts.md book mark |
| D-23 | 2026-09-24 | P-11 | post_exit_position_view.py:28 and orchestrator.py ~1725 value a hypothetical position at the mid. | Open. Rung P-12 moves them to the executable side, with its own census and pre-registered prediction. P-11 gives the views delegation-only methods. | It changes numbers on VALID quotes too; whether they feed a verdict is unmeasured. | phase14 plan P-12 row |
| D-24 | 2026-09-24 | P-11 | Should the position store re-apply the quote-quality rule? | No. The orchestrator's mark path, the only production writer of marks (static test T7), applies the full rule. The store rejects only a non-positive side, retaining the last valid bid/ask. | The store never sees quote sizes, so it cannot apply the rule fully; a partial copy would be a second, disagreeing source of truth for D-21's single rule. Provenance: surfaced at P-11 Stage 0 (a determinism fixture writes a locked pair directly to the store). | contracts.md book mark |
| D-25 | 2026-09-24 | P-11 | Does a mid-only update_mark still create unrealized P&L? | No. Mid is retired as a valuation input everywhere; a mid-only update sets reference_mid only. Tests that used a mid-only update to stage a valuation now pass the executable side at the same price (setup lines only, assertions byte-identical); the two tests whose subject was mid-versus-side valuation assert the contract instead. | No production caller passes mid-only (static test T7: the orchestrator is the only writer and always passes bid/ask). Keeping a reachable mid fallback would let a future caller value at mid silently, contrary to the contract. | contracts.md book mark | Sweep at P-11 applied the setup rule to: tests/conformance/test_reduction_permitted.py. |
| D-26 | 2026-09-24 | P-12 | How do the post-exit views value a hypothetical position? (closes D-23) | Unrealized at PositionStore.valuation_mark(symbol, new_quantity): bid for a long, ask for a short, by the hypothetical direction; no side → 0 (entry). Exposure notional stays reference_mid. The static guard also allowlists two notional readers found at P-12 Stage 0: bootstrap._position_lookup, Orchestrator._record_portfolio_net_shadow. | Valuation is executable-side (D-22). Exposure is computed as total − old + new contribution; the total uses reference_mid, so one price must be used throughout or notional is created or lost. | contracts.md book mark |
| D-27 | 2026-09-24 | P-12 | risk/post_exit_position_view.PostExitPositionView has no production caller and duplicates orchestrator._PostExitPositionView. | Open. Consolidate in a later hygiene rung; P-12 changes both identically and tests both. | Consolidation touches import boundaries (kernel ↔ risk) and is not a correctness change. | — |
| D-28 | 2026-09-24 | P-20 | How are "held quotes" and the feed-gap injection realised in the tape fixture? | hold() repeats the held quote's prices on the following quotes, keeping their timestamps and sequences (count preserved, so injected and clean tapes pair event by event). The feed-gap injector is deferred to P-21: the flag lives on the rail's output, not on NBBOQuote. | Pairing clean and injected tapes by sequence is what member 6 compares; the feed-gap flag has no carrier until the rail surface is exercised. | battery.md fixtures |
| D-29 | 2026-09-24 | P-20 | Battery member 3 states "share exiting favorable first = a / (a + b)" without defining a and b. | Open. P-21 defines them explicitly in battery.md before any member depends on them. For a symmetric walk, P(reach +u before −d) = d / (u + d); the stated formula is correct only if a is the adverse distance. The generator test (G3) verifies the walk property itself. | A known-answer member is only as good as its stated answer; the convention must be fixed in words before code relies on it. | battery.md member 3 (at P-21) |
| D-30 | 2026-09-25 | P-21a | Where do battery_real members run? (corrects evaluation.md §5) | Only in the parity oracle job, cache required; check and the local merge gate deselect them. The CI edit lands with the first battery_real member. | The check job restores but does not require the cache, so a miss would skip silently; an empty marker selection exits 5. | evaluation.md §5 |
| D-31 | 2026-09-25 | P-21a | How does member 1 reverse gate order, detach sinks, run a fresh process and inject a feed gap? | PositionEngine.gate_order (default ADVERSE, FAVORABLE); sink attachment optional; fresh process, canonical serialisation and feed-gap injection (wrapping MarkRail.on_quote) are test-side. | The battery named the perturbations but the contract named no way to perform them; tests cannot land before a seam they depend on exists in the spec. | contracts.md §8; battery.md fixtures |
| D-32 | 2026-09-25 | P-21a | Define a and b in member 3 (closes D-29). | u, d = favorable and adverse trigger distances in ticks from the valuation mark at birth; share favorable-first = d / (u + d); thresholds from entry cost translate as u = X + s, d = A − s. | The first reading is −s (A-15), so distances from entry cost and from valuation differ by one spread; the known answer must name its reference point. P-20 G3 verified P(+u before −d) = d/(u + d) on the oracle. | battery.md member 3 |
| D-33 | 2026-09-25 | P-21a | A reads_no_sensor alpha receives no horizon boundaries unless some sensor is configured (bootstrap builds the scheduler only with a sensor consumer). | Open (hygiene). The fixture schedule test configures the P-15 sensor set solely to obtain boundaries. A later rung decides whether the scheduler should be built for any horizon-bound alpha. | Changing bootstrap is outside this campaign's stage A and would need its own census and prediction; the test-side workaround reads no sensor value. | — |
| D-34 | 2026-09-25 | P-21b | Which synthetic tapes do members use, and how is member 4's touch clause covered? | Member tapes start at 2026-03-26 14:00 UTC with size 1000 (orders fit the touch); a size-100 tape exercises partial fills. The touch clause is conditional on every entry fill printing at the birth paying mark; its coverage count is reported. | The market filler adds a within-L1 premium (market_fill.py ~209-225), so no fill prints at the raw touch today; asserting the clause unconditionally would test the fill model, not the engine. | battery.md member 4 |
| D-35 | 2026-09-25 | P-21b | Split P-21b. | P-21b: harness, APP config, CI, members 1, 2, 4. P-21c: members 3, 5, 6 after a census of the gate definitions (Q, D, band draw, precedence). Both before P-30. | Members 3, 5, 6 freeze gate semantics; they are written from quoted contract text, not recalled. | phase14 plan |
| D-36 | 2026-09-25 | P-21b | Where and how does member 1's multi-name perturbation run? | Synthetic tapes only; the second name ("ZZZ") is quoted but never traded, because per-alpha Top1SelectionPolicy keeps one same-tick signal and ZZZ sorts after SYN. The test asserts ZZZ has zero orders as a precondition, then compares SYN's records after dropping sequence-type fields and cell_id. | The cache holds one real name. With single-slot selection a name's fills legitimately depend on the universe, so only independence from another name's quote flow is a property of engine 13; sequence numbers interleave legitimately. | battery.md member 1 |
| D-37 | 2026-09-25 | P-21b | Member 4's rail half: red until C? | Green from A. | The P-10 rail stub already computes executable-side orientation (paying ask / valuation bid for a long; worst and forced = min(bid, ask)), which is what the half asserts (census P-21 D2). | battery.md stage gates |
| D-38 | 2026-09-25 | P-21b | Is cross-name independence of two simultaneously open positions tested? | No — recorded limitation. Under per-alpha top-1 selection this fixture cannot hold positions in two names at once. Revisit if a multi-slot selection policy or a per-symbol alpha scope is introduced. | Claiming coverage the setup cannot produce would be a false guarantee. | battery.md member 1 |
| D-39 | 2026-09-25 | P-21b | How is a record attributed to the tape event that caused it? | A cursor moves on MarkRailUpdate (to its quote_sequence) and on NBBOQuote (to its sequence); every record takes the cursor at publication. Implemented as a pure function with its own test. | The kernel publishes the rail update before the quote (P-10), so engine outputs for event k precede quote k on the bus; a last-quote cursor would attribute them to k−1 and make member 2's prefix comparison fail for a correct engine. | scenarios.py; battery.md member 2 |
| D-40 | 2026-09-25 | P-21c | Where do S and D live, and what are their defaults? | Platform config `position_rail_slippage_ticks: int = 0` (S, ticks) and `position_rail_dwell_ns: int = 0` (D, ns); omitted from the config snapshot when at default. Load check (L8 platform half): S ≥ 0, D ≥ 0. Implemented at P-40. (contracts.md §9) | A-07 names S and D as run config but not the platform fields; a non-default zero would move the config snapshot for no semantic change. | contracts.md §9 |
| D-41 | 2026-09-25 | P-21c | What does the dwell window hold, including when D is 0? | Holds every quote with `exchange_timestamp_ns` in `(t − D, t]` and always the current quote. With D = 0 it holds only the current quote: `dwelled_exit_mark` equals `valuation_mark` and `warmed_up` is true from the first quote. (contracts.md §9) | The open interval `(t − D, t]` excludes the quote at t, so D = 0 would hold nothing and the favorable mark would be undefined. | contracts.md §9; battery.md member 2 |
| D-42 | 2026-09-25 | P-21c | What is A, and when does the adverse gate fire BLIND? | `A = adverse.blind_limit_seconds` (ns on the manifest). The adverse gate fires `BLIND` when, for the cell's orientation, `paying_absent_for_ns > A`, `valuation_absent_for_ns > A`, or `symbol_quiet_ns > A`. (contracts.md §9) | D-32 used A as the adverse distance; the contract uses A as the blind limit. The two must not share a letter. | contracts.md §9; battery.md member 3 |
| D-43 | 2026-09-25 | P-21c | What are the favorable suppression reasons, and in what order? | Checked in this order, the first failing one reported, all six recorded in the flag state: `VALUATION_SIDE_ABSENT`, `CROSSED`, `FEED_GAP`, `DWELL_NOT_CLEAN`, `NOT_WARMED_UP`, `SYMBOL_QUIET`. (contracts.md §9) | The contract lists the hard blocks but not the reason tokens or which one is reported when several apply. | contracts.md §9 |
| D-44 | 2026-09-25 | P-21c | How is the adverse band level drawn? | `cell_id` is `symbol`, `strategy_id`, `birth_fill_sequence`, `side` joined by a bar (contracts §2, verbatim in §9). `L = (centre − B/2) + (int.from_bytes(sha256(cell_id.encode("utf-8")).digest()[:8], "big") mod (B + 1))`. Recomputed each event from `cell_id` and frozen config; never stored. (contracts.md §9) | The contract seeds the draw with SHA-256 of `cell_id` over the whole-tick band but does not give the integer a test can recompute. | contracts.md §9 |
| D-45 | 2026-09-25 | P-21c | What is the horizon deadline when no session close resolves? | When no session close resolves, `horizon_deadline_ns = birth_fill_ts + T`. (contracts.md §9) | The frozen-at-birth formula takes a session close; a tape with no session fact would otherwise have no deadline. | contracts.md §9 |
| D-46 | 2026-09-25 | P-21c | How are battery result, displacement and cost computed? | Per closed cell, from raw prices, no fee: `result = sign × (Σ exit fill price × qty − Σ entry fill price × qty)`; `displacement = sign × size × (valuation mark at the quote of the exit fill − valuation mark at the quote of the entry fill)`, taken from the tape; `cost = displacement − result`. Fees never enter engine figures; `fee_round_trip_ticks` enters only `round_trip_ticks`. (contracts.md §9) | Member 3's conservation identity names a cost the engine is forbidden to emit; the battery computes it from the fills and the tape. | contracts.md §9; battery.md member 3 |
| D-47 | 2026-09-25 | P-21c | What is the closed set of exit reasons, and how are the closing-record flags set? | Exit reasons: `ADVERSE`, `HORIZON`, `INVALIDATION`, `FAVORABLE`, `END_OF_TAPE`, `EXTERNAL:SIGN_FLIP`, `EXTERNAL:SAFETY` (engine 8), `EXTERNAL:OTHER`. Requirement reasons map ADVERSE to ADVERSE_EXCURSION, HORIZON to HORIZON, INVALIDATION to INVALIDATION, FAVORABLE to FAVORABLE_EXCURSION. Flags at the deciding rail event unless stated: `closed_on_stale_data` (valuation side absent, or `symbol_quiet_ns > Q`); `exited_on_unusable_data` (trigger `BLIND`); `lived_through_feed_gap` (any rail event in the cell's life had `feed_gap_before`); `first_event_exit` (the deciding event is the cell's first rail event after birth); `stop_inside_round_trip` (drawn `L ≤ round_trip_ticks`); `target_inside_round_trip` (the fixed form was disarmed at birth, L1 runtime clause). (contracts.md §9) | Exit reasons and flag predicates are named in prose across the contract but not as a closed set a member can assert. | contracts.md §9 |
| D-48 | 2026-09-25 | P-21c | Split P-21c. | Split: P-21c spec closure (docs); P-21d members 3, 5 (+ tape injectors set_quote, excise); P-21e member 6 after a design note. | members freeze semantics; member 6's v2 text conflicts with contract §3 on held quotes and with fail-passive gating on the result clause. | phase14 plan |
| D-49 | 2026-09-25 | P-21d | Why do V3a fills drop in hours 5 and 6 of a 6h tape? | Per-alpha drawdown. The fixture `risk_budget.max_drawdown_pct` is 5.0. `AlphaRiskEngine.check_signal` rejects with `per-alpha drawdown {pct}% >= limit 5.0% — alpha should be quarantined` (`src/feelies/risk/risk_wrapper.py`, the drawdown return at the `drawdown_pct >= budget.max_drawdown_pct` branch). First affected signal: timestamp 1774549050000000000 (2026-03-26 14:17:30 ET). Session bounds on the synthetic run are None; the clock is not the cause. Tapes for members 3 and 5 stay at or under 4h, before this reject. Headroom at stage A, every synthetic m3/m5 run, limit 5%, rejects by reason none, first reject none: m3_barriers seed 11 quotes 120000 entries 200 closed 0 peak 3.4021%; m3_swapped seed 17 quotes 120000 entries 200 closed 0 peak 2.3465%; m3_deadline seed 19 quotes 120000 entries 200 closed 0 peak 4.1001%; m3_band seed 23 quotes 120000 entries 200 closed 0 peak 4.3478%; m5_invalidation seed 29 quotes 4000 entries 7 closed 0 peak 0.0595%; m5_deadline seed 29 quotes 4000 entries 7 closed 0 peak 0.0595%; m5_gap seed 29 quotes 3700 entries 7 closed 0 peak 0.0595%. Tightest use of the limit is 4.3478/5 = 86.96%. | A logged risk reason is a named mechanism. The battery tapes stop before it so the drop is not mistaken for an engine defect. | — |
| D-50 | 2026-09-25 | P-21d | Census V3a uses T_seconds 7200, which is shorter than a tape long enough for 200 cells. | Member 3 clauses (a) and (c) use T_seconds 16000. The tape is 120_000 quotes (12_000s). 16000s is longer than that span. Load: AlphaLoader.load_from_dict accepts the edited fixture (same path as V3a, T_ns 16000000000000). | Battery.md says T longer than the tape wherever there is no deadline. 200 odd 30s boundaries need 12_000s. | battery.md member 3 |
| D-51 | 2026-09-25 | P-21d | Where does the deadline-and-stop collision put the barrier? | On the deadline quote itself (birth fill index + T/interval), valuation exactly d ticks adverse of the birth valuation. C2's q_b+1 placement is for an exit decided by a Signal on q_b. A deadline is the rail event of that quote, so the stop and the deadline are the same event only if the price is on that quote. | Member 5's "deadline landing on an event beyond the stop" is one rail event, not a signal-versus-mark ordering. | battery.md member 5 |
| D-52 | 2026-09-25 | P-21d | How is a gap through both a trail and a stop built? | excise 300 quotes after the entry fill (sequences kept; the time gap is 300 × 0.1s), then set_quote the first quote after the excision so the long valuation is 20 ticks past the adverse barrier. One print below both the stop and a trail that sits above it. | Member 5's gap collision. C3 keeps the original sequences; C2 supplies the price. | battery.md member 5 |
| D-53 | 2026-09-25 | P-21d | May a risk-layer reject silently thin a battery sample? | Risk-layer rejects must not silently thin a battery sample. Synthetic members assert zero risk rejects after nonvacuity (`assert_no_risk_rejects`). If the guard fires at a later stage, the fix is a recorded change to the synthetic config (e.g. the drawdown limit), never a weaker assertion or a shorter tape chosen after the fact. | A sample that lost its losing paths to a risk reject would make conservation and precedence look cleaner than the tape is. | scenarios.py |
| D-54 | 2026-09-26 | P-21e | What does member 6 mean by a held quote? | Excision. A held quote is a quiet name: the quotes are removed and the timestamps are not repeated. `hold` repeats prices and is dwell-only; it is not the member 6 injection. | D-48: held quotes must mean a quiet name (quotes excised), not repeated quotes. | battery.md member 6 |
| D-55 | 2026-09-26 | P-21e | How is a gap through the adverse barrier built, and where does BLIND fire? | (a) Prices are real tape prices. (b) The gap-through is excise plus shift_from. The exit fills on q_g+1. A gap shorter than A is ADVERSE and strictly worse than the barrier. set_quote on q_g alone reverts on q_g+1 and is not enough (F2). Seed 11, V3a: excise 20 quotes from sequence 303, then shift_from −15 cents from sequence 323, gives q_g 99.76/99.77 and q_g+1 99.75/99.76. (c) The same strict greater-than A rule covers symbol_quiet_ns, valuation_absent_for_ns and paying_absent_for_ns, and exactly A must not fire. At a 100 ms interval, excise 298 is 29.9e9, 299 is 30.0e9 (exactly A), 300 is 30.1e9 (fires). | F1: an order decided on q fills on q+1 at the executable side. F3: BLIND is strict greater than A. | battery.md member 6 |
| D-56 | 2026-09-26 | P-21e | Where do member 6 injections sit? | On the test side. A feed-gap seam is a test-side wrapper of MarkRail.on_quote that sets feed_gap_before on exactly one update. Nothing in BACKTEST overwrites that flag (F4). Crossed, locked and removed-side quotes are tape injectors. | Fresh-process runs, canonical serialisation and feed-gap injection need no production hook (contracts.md §8). | battery.md member 6 |
| D-57 | 2026-09-26 | P-21e | May member 6 assert session-level favorable and adverse counts? | No. Local path-wise assertions replace session counts. An injection shifts one cell's exit, which shifts later births; session-level favorable and adverse counts can move either way for a correct engine. A6 is enforceable by the rail side-usability rule (D-62). | A count that can move either way for a correct engine does not catch a hole. | battery.md member 6 |
| D-58 | 2026-09-26 | P-21e | How are the real and synthetic injections split, and where do they sit? | (R) one clean real session and three injected copies. (S) synthetic constructions on the C_SYN harness. Placement rule P: an injection sits at event e inside a live cell's life where the clean run makes no exit decision at e or e+1. Eligible cells are ranked by sha256(cell_id); up to 5 injections per copy. Deterministic. | An injection on a quote the clean run already exits on is not a comparison of the hole. | battery.md member 6 |
| D-59 | 2026-09-26 | P-21e | Where does the synthetic per-alpha drawdown of 100 live? | In the harness, not the fixture. Every run_synthetic sets risk_budget.max_drawdown_pct to 100.0 on the in-memory spec unless the caller's variant sets that key to a value other than the on-disk 5.0. run_real is unchanged. The fixture file stays 5.0 and is shared with configs/bt_position_arbitrary_not_calibrated.yaml. S0-4 hashes match the census. The D-53 guard stays. | F5: synthetic record hashes are byte-identical at 5, 20 and 100. Raising the fixture would change the real session. | scenarios.py |
| D-60 | 2026-09-26 | P-21e | Are post-merge baseline captures tracked? | No. Post-merge captures are untracked and ignored (baseline_post-merge-*.json). The ledger records the compare line. Files are kept in ..\feelies-captures\<rung>\. | A merge capture is an operator artifact, not a rung input. | .gitignore |
| D-61 | 2026-09-26 | P-21e | What keeps the registries and the ledger from silent duplicates? | Registries count exactly once: a repeated id fails. The ledger has no byte-identical blocks. Re-entries with the same id and distinct text are legal. | A copied block reads as a second decision and is not an attempt history. | tools/exec/doc_integrity.py |
| D-62 | 2026-09-26 | P-21e | When is a rail side usable? | Rail side usability follows the store's enforcement. For every quote the rail publishes an update; a side the orchestrator mark path refuses for the store is marked absent on the rail, and its absence clock runs from the last usable value. symbol_quiet_ns measures feed silence only and resets on any quote. Crossed and locked books are absent on both sides. Rationale: a crossed book inflates the bid and depresses the ask, so it flatters both orientations' valuation side and can conceal a stop breach. §9 BLIND fires only on absence or quiet, so without this rule a persistent crossed book is never BLIND. Implementation (P-40): quote classification is computed once in the orchestrator mark path and passed to both store and rail; there is no second classify call site. Found by P-21e S0-1 (the P-10 stub publishes crossed/locked with both sides present). | The P-10 stub publishes crossed and locked quotes with both sides present, so absence clocks stay at 0 and BLIND cannot see them. | contracts.md §9; battery.md member 4 |
| D-63 | 2026-09-26 | P-13 | Where do backtest regime-calibration quotes come from? | Rule A, unconditional, no config switch. Calibration quotes are the RTH quotes, all universe symbols, of the prior session. The prior session is the previous date from iter_trading_dates (weekday walker; for 2026-03-26 it is 2026-03-25), capped at regime_calibration_max_quotes, in the same order prep uses today. The replayed session contributes none. Backtest uses this now. Live and paper get the same shared function in the paper/live campaign; those entry points are not wired here. Shared function: feelies.harness.regime_calibration.prior_session_calibration_quotes(*, symbols, session_date, max_quotes, loader) -> (quotes, provenance). Pure given its loader. provenance = (source_date or None, n). | A fit on the session being replayed uses quotes that had not arrived yet at the earlier quotes of that session. The prior session is already over. | harness/regime_calibration.py |
| D-64 | 2026-09-26 | P-13 | What do BudgetBasedSizer._get_regime_factor and BasicRiskEngine._regime_scaling return when RegimeState.calibrated is False? | min(scales). Gates stay fail-closed. Real paths: src/feelies/core/position_sizer.py and src/feelies/risk/basic_risk.py. | An uncalibrated posterior carries no more information than a missing one, whose convention is already min(scales). Sizing at 1.0 would maximise size under an unknown regime. | core/position_sizer.py; risk/basic_risk.py |
| D-65 | 2026-09-26 | P-13 | What does widened member 2 compare, and where is the prefix cut? | All bus types except MetricEvent and StateTransition. MetricEvent is telemetry and can embed wall-clock durations (tick_to_decision_latency_ns), so two replays of one prefix are not bit-identical. StateTransition includes session-boundary transitions (BACKTEST_COMPLETE, SESSION_FEED_COMPLETE, CMD_SHUTDOWN) that fire because the run ended, so a truncated run emits them at the cutoff and the full run emits them only at the true end. The boundary is the replay event index. Each kept event is a streaming per-event sha256 digest; the first divergent type and cursor are the failure. | A snapshot-only prefix cannot see a regime posterior fit on quotes after the cutoff. | scenarios.py; pending docs rung (battery.md member 2) |
| D-66 | 2026-09-26 | P-13 | May the legacy oracle be re-pinned for causal regime calibration? | Yes, once. A one-time pre-registered re-pin of exactly the three constants in the prediction block under this entry. Evidence: P-13 census B. The oracle is frozen again after this rung. | A declared break is written before the pre-capture. After this rung the freeze holds. | evaluation.md §1 |
| D-67 | 2026-09-26 | P-13 | What happens when the prior weekday has no tape? | Fallback C. calibrated=False, and the miss is logged. The weekday walker has no exchange holiday calendar, so a prior weekday that was a holiday has no data and takes fallback C. An exchange holiday calendar is a platform gap. | A holiday is not a session. Inventing quotes for it would be a second source of data. | — |
| D-68 | 2026-09-26 | P-13 | Does the calendar_spans ex-date guard have to become causal in this rung? | No. Reviewed and accepted: it is a non-causal boot validity guard with no decision use. | It does not choose a trade. | — |
| D-69 | 2026-09-26 | P-13 | D-65 cut rule superseded: where does widened member 2 cut the prefix, and what wall budget does that comparison have? | Cuts sit at decision points. From the full run, for each target position, the prefix ends at the replay index of the triggering event of the first OrderRequest at or after that position, including that event and nothing after it. Synthetic (seed 11, n 36000): 4 cuts, targets 20 / 45 / 70 / 90 percent of the tape. Real APP (fixture config): 3 cuts, targets 15 / 30 / 45 percent of the RTH replay. The full-run digest sequence is computed once per session (module-scoped cache) and reused for every cut. The test asserts the chosen cut indices are strictly increasing and that each is a decision trigger. Budgets: synthetic member 2 total wall ≤ 20 s (local); CI step "Battery members (real session)" ≤ 60 s. | A peek of any distance is caught by construction: the prefix ends on the event that triggered the decision. Fixed fractions catch a local peek only by chance, when a fraction happens to land after the affected decision and before the peeked quote. | scenarios.py; pending docs rung (battery.md member 2) |
| D-70 | 2026-09-26 | P-13 | PROCESS: what must a fail-first re-proof show before the detector's verdict is read? | The injected defect must be shown to have taken effect — for example the calibration provenance (source date and n) on each run — before that verdict counts. A green or red result on a run that is still on the committed calibration path does not re-prove the detector. | A re-proof that never leaves the path under test cannot go red, and a green result then condemns a detector that was not exercised. | — |
| D-71 | 2026-09-26 | P-13 | Widened member 2 CI budget criterion: what must the parity oracle job satisfy? | The parity oracle job must not extend the CI critical path (its total wall ≤ the check job's). This replaces the ≤60 s step target in D-69, which was set without reference to that purpose. Measured on run 36238122317 at b291d4d2: check (ubuntu-latest) 11:12:55Z–11:21:34Z, 519 s; parity oracle 11:12:55Z–11:15:37Z, 162 s; step "Battery members (real session)" 11:13:39Z–11:15:02Z, 83 s. The parity job is the shorter one. | The check job is what the critical path waits on. A step cap that the parity job can miss while still finishing first does not protect that path. | — |
| D-72 | 2026-09-26 | P-21f | How does a test force a quote into a class that `set_quote` and `cross` cannot express? | `force_class(tape, index, cls)` is a pure test injector. `cls` is one of NONPOS_BID, NONPOS_ASK, CROSSED, LOCKED, ZERO_SZ_BID, ZERO_SZ_ASK, and the quote at that index is replaced with the canonical example for the class (NONPOS bid 0/100.02, NONPOS ask 100.01/0, CROSSED 100.50/100.00, LOCKED 100.00/100.00, ZERO_SZ sizes 0/1000 and 1000/0). Timestamp and sequence stay. The input list is not mutated. `ValueError` if `classify` does not return the class the name claims. | `set_quote` requires 0 < bid < ask, and `cross` requires bid > ask, so NONPOS and LOCKED have no existing injector. ZERO_SZ keeps a valid spread and only the size changes. | tests/position_engine/tapes.py |
| D-73 | 2026-09-26 | P-21f | Where does a real-session quote transform sit, and how is it cached? | `run_real(..., fraction=None, quote_transform=None, rail_wrapper=None)`. The fraction slice is applied after `end_index` and before `append_batch`. `quote_transform` runs on that slice, still before `append_batch`. `rail_wrapper` is the existing feed-gap seam. A set transform or rail is keyed by `repr` and does not share the plain-run cache entry. An unset transform keeps the plain cache. | The injection has to see the same events the engine replays, and a transformed run must not be served as a clean one. | tests/position_engine/scenarios.py |
| D-74 | 2026-09-26 | P-21f | How is placement rule P computed from a clean run? | A cell life is the inclusive replay-index span from the birth quote to the deciding quote. The midpoint is `birth + (exit − birth) // 2`. The chosen event is the first index at or after that midpoint that is inside the span and is not itself an exit decision, and whose next index is not an exit decision. Cells with no such index are dropped. Eligible cells are ranked by `sha256(cell_id)` and at most five are kept. If the chosen replay index is not a quote, the injector snaps forward to the next quote that is still eligible. | The rule is pure in the clean run's cells, so two sessions with the same closes place the same injections. | tests/position_engine/scenarios.py |
| D-75 | 2026-09-26 | P-21f | Why does A2 also require the RiskVerdict streams to be equal? | A2(b): the clean and injected `RiskVerdict` streams are equal, as `(sequence, symbol, action, reason, scaling_factor)`. Under P-13 rule A the real fixture has risk rejects. An ignorable injection cannot legitimately change exposure, so a verdict that appears, disappears, or moves is a failed injection, not a passed A2. | A comparison of exit tuples alone would accept a run whose risk engine changed size because of the injected quote. | tests/position_engine/scenarios.py |

### D-66 pre-registered prediction

```
Moves (exactly these three of the 64):
  _BASELINE_FILL_COUNT         20 → 10
  _BASELINE_NET_PNL            103.93 → 24.61
  _BASELINE_TRADE_PARITY_HASH  0601295a20b518ea4b6997cbd1aff145049570de044a0766a16b566a3ba17df3
                               → 18f6bb4ecd7b1b1aad5158077cd6ebbc3e6db27fdab2e2effaf5a74e98545bfb
Unchanged: every other constant, including _BASELINE_CONFIG_HASH (bb67b1c7…) and _BASELINE_DATA_VERSION
(cache:2364ef7fe41c27d9).
Direction: fills down, net down.
Mechanism: regime emissions fitted on 2026-03-25 RTH (50636 quotes) instead of 2026-03-26.
Any other constant moving, or any predicted value not reproduced exactly → STOP, status reverted.
```

## Findings

| Id | Date | Rung | Finding |
|---|---|---|---|
| F-P13 | 2026-09-26 | P-13 | The legacy oracle's net fell 103.93 → 24.61 under causal calibration. Any research result on the regime-gated or regime-sized backtest path before P-13 is to be re-run causally before campaign 15 relies on it. |
| F-P13b | 2026-09-26 | P-13 | test_two_alphas_hold_live_targets_on_one_symbol previously passed only through the removed same-session scan. Its fix is the D-63 live wiring (paper/live campaign). Non-gating. |
| F-P13c | 2026-09-26 | P-13 | The populate step's 2026-03-25 backtest exits 2 because 2026-03-24 is uncalibrated (D-67). Handled per B6: populate calls ingest_data, which writes the cache and does not run that backtest. |
