<!--
  File:   docs/research/prompt_pack_11_hypothesis_slate_d.md
  Status: hypothesis — DECIDED (2026-07-16): H12 CONFIRMED for Task 7
          path; H13 HELD CONTINGENT under enumerated triggers
          (DISPOSITIONS 1–4). Fourth hypothesis slate (cycle 3,
          FINAL): exactly two SCHEDULED_FLOW candidates, passive,
          H ∈ {900, 1800}. Pre-registration class; N = 12 unchanged
          (selection does not increment; first outcome contact on
          any primary remains +1 → N ≥ 13).
  Owner:  microstructure-alpha (cards) / research-workflow (trial
          ledger); prompt-pack Task 6-D, Phase B.

  Provenance (FQ-3 template):
    git_sha: "58c6fc60e9b8fa7b703076187f61d65d37048082" (HEAD at task
      start; this file is the sole intended output)
    worktree_clean: "yes at task start (git status --porcelain empty)"
    pythonhashseed: "0 (set for the census-legal clock-geometry
      boundary count below; no σ / IC / forward-return contact)"
    normative_inputs:
      prompt_pack_08_frontier_refresh.md (operative κ / floors /
        density basis; §2.4 / §6 pre-filter),
      prompt_pack_10_cycle2_retrospective.md (DISPOSITION: cycle-3
        authorization + program stop-rule; §4.2 SCHEDULED_FLOW open;
        outcome-informed prior disclosure rule),
      prompt_pack_09_hypothesis_slate_c.md (+ DISPOSITIONS 3, 8 —
        H11 form FAIL / H11′-class revival bar),
      prompt_pack_09a_slate_c_review.md (§2 H11 FAIL rationale;
        §2 density actuals on 140-cell cache),
      prompt_pack_03b_print_eligibility.md (§3.3 Class-A, §4.4),
      prompt_pack_03c_universe_and_cache.md (through AMENDMENT 2:
        140 ingested; ENSG/MLI DRAWN-NOT-INGESTED; HOLIDAY-THIN),
      prompt_pack_00b_edge_units_convention.md (one-way),
      prompt_pack_07_program_retrospective.md (cycle-1 funnel;
        H4 close-window park — not an economic prior),
      prompt_pack_03m_skill_verification.md + research-protocol
        Validation Protocol & Slate Design Discipline (3-M:
        magnitude-vs-power, consequence-precedence, occupancy
        pre-read),
      prompt_pack_backlog.md entries 17–18 (failure-mode triggers;
        REPORTS estimand labeling),
      src/feelies/alpha/layer_validator.py
        (_FAMILY_FINGERPRINT_SENSORS SCHEDULED_FLOW →
        scheduled_flow_window),
      src/feelies/storage/reference/event_calendar/__init__.py
        (WindowKind taxonomy — five kinds today).
-->

# Prompt-pack Task 6-D — Hypothesis slate D (cycle 3, final)

Pre-registration document. Exactly **two** candidate SIGNAL-layer
hypotheses in family `SCHEDULED_FLOW`, passive, H ∈ {900, 1800},
written before any outcome contact for these candidates (Inv-2).
Shipped alphas, parked/rejected H1–H11 cards, and gas decisions are
mechanics/convention pointers only — not economic priors (session
constraint 6). Every number below is quoted from a committed artifact
or a stated design prior (flagged).

**Authorization / stop-rule (pack-10 DISPOSITION, binding):** cycle 3
is AUTHORIZED for this region only; it is the **FINAL** cycle on this
universe/grid. Absent a step-2b PASS by any cycle-3 card, the program
closes. Kill-gate class does not matter for closure. **N = 12** at
slate drafting; first outcome contact on any primary below is
**+1 → N ≥ 13**.

**Operative frontier:** pack-08 (FQ-9). H2 lesson: open at own frozen
κ on the **median** session (honest κ ≈ 0.16 class). H11 lesson
(09a §2 / pack-09 DISPOSITIONS 3): `SCHEDULED_FLOW` without
`scheduled_flow_window` in `l1_signature_sensors` and without a
clock/window predicate in the conditional is a **form FAIL** —
reformalization is a NEW card meeting the full bar, not a patch.

---

## 0. SLATE SHAPE — two cards; SCHEDULED_FLOW only

Forced by pack-10 DISPOSITION 1 and pack-08 §2.4 / §6 — not chosen for
convenience:

1. **KYLE_INFO continuation** on this grid: closed for program
   purposes (Interpretation B, pack-10 DISPOSITION 1). Not drafted.
2. **INVENTORY / HAWKES / H=30 / taker:** still closed (pack-08 §3).
3. **Open authorized region:** `SCHEDULED_FLOW` **passive** at
   H ∈ {900, 1800} only.
4. **Exactly two cards** — the region is narrow; a third would be
   filler. One may be H11′-class.
5. **Structural-distinctness rule (task):** each claim must be
   scheduled-flow **in substance**. The falsifiable window-binding
   clause (F2-class: effect at matched conditioning **outside**
   registered windows refutes) is **mandatory**. A continuation claim
   with a clock decoration fails review by construction.
6. **Density bar (D-C1 convention):** design-central ≥ **130**
   contamination-excluded episodes on the card's evidence pool (30 %
   margin over the pooled census floor ≥ **100**). HOLIDAY-THIN
   haircut **18/20 = 0.90** on every 20-session symbol (03c A1.4 /
   A2.3). ENSG/MLI remain at 10 sessions (HT = 1.0 on their dates —
   no HOLIDAY-THIN tags in the original ten).
7. **H = 1800:** per-symbol power cannot clear 130 on this boundary
   basis (pack-08 §4.1; 09a §2). Any 1800 card MUST declare a pooled
   evidence structure. Additionally, **half-hour marks are
   tautological on the H = 1800 decision grid** (see §0.2) — an
   H = 1800 card may not use “all half-hour marks” as its window set.

### 0.1 Conventions binding on every card

- **Units:** one-way, per-fill, bps of fill notional (00b).
- **Cost floors (pack-08 §2.1):** APP **4.68**, RMBS **5.51**, OLN
  **8.69**, ENSG 5.04, DIOD **6.23**, PCTY **5.19**, MLI 5.32, CROX
  **5.66** bps passive.
- **σ_H / κ_req:** pack-08 §2.2 for APP/RMBS (n = 20). For
  {OLN, DIOD, PCTY, CROX, ENSG, MLI}: pack-08 / pack-05 10-session
  surface (bit-identical in FQ-9 for the then-non-expanded six).
  Tranche-1B expanded four symbols to 20 sessions **without** an
  FQ-9-class σ refresh — deployability at design cites the committed
  10-session κ_req; census pre-registers park if measured median
  κ_req on the operative 20 exceeds κ_frozen (coverage, not tuning).
- **Pre-filter (pack-08 §6):** enter ranking only if
  (family × H × symbols × execution) is FEASIBLE at the card's own
  frozen κ on the **median** session.
- **Session window:** 09:35–15:50 ET; in-window boundaries =
  **25 / session at H = 900**, **12 / session at H = 1800**
  (pack-08 §1 / §4; 09a §2 bit-exact on the 140-cell cache).
- **Warm:** no load-bearing `spread_z_30d`. Window-sensor warm is
  **calendar-warm** (see each card block 4) — distinct from quote/trade
  warm.
- **Contamination:** quote-fed conditioners carry 03b posture as
  stated; NEW trade-fed extremes would need Class-A + §4.4 (neither
  card uses trade-fed entry). REPORTS estimands labeled at freeze
  (backlog 18).
- **L1–L5 (03c):** inherited. OQ-3 caveat: mechanism-share runtime
  not active. Vendor V-1: grid inside pre-2026-04-27 cap.

### 0.2 Clock-geometry fact (census-legal; binding on window design)

`PYTHONHASHSEED=0` boundary enumeration on the 09:30-ET-anchored grid
restricted to 09:35–15:50 ET (same nominal grid as pack-08 / 09a;
**no mid returns, no σ, no IC**):

| H | in-window boundaries / session | half-hour marks (:00/:30) | on-the-hour marks (:00) |
|---|---|---|---|
| 900 | **25** | **12** (frac **0.48**) | 6 (frac 0.24) |
| 1800 | **12** | **12** (frac **1.00**) | **6** (frac **0.50**) |

**Consequence:** an H = 1800 card whose windows = all half-hour marks
has an **empty out-of-window contrast set** on the decision grid —
F2 window-binding is vacuous by construction. That design fails the
structural-distinctness rule. H11's drafted H = 1800 + half-hour story
is therefore unreformalizable **at that horizon with that window set**;
H11′ must change horizon and/or window subset so F2 is testable.

### 0.3 H11 reformalization bar (09a §2 / pack-09 DISPOSITIONS 3, 8)

A NEW card meets the bar only if **all** of:

1. `trend_mechanism: SCHEDULED_FLOW` with
   `l1_signature_sensors` containing **`scheduled_flow_window`**
   (G16 rule-5; sole primary in
   `_FAMILY_FINGERPRINT_SENSORS["SCHEDULED_FLOW"]`).
2. The conditional-distribution statement contains an explicit
   clock/window predicate (`scheduled_flow_window_active` / window-kind
   membership) — not merely a prose story around an OFI tail.
3. **Calendar-artifact deliverables are named** (per-session YAML under
   `src/feelies/storage/reference/event_calendar/`, content-addressed;
   today only three fixture dates exist).
4. F2 window-binding is load-bearing (matched conditioner outside
   windows refutes).

Drafted H11 (`ofi_ewma` signature, no clock predicate) remains
NOT SELECTED text — not patched in place.

### 0.4 Platform gap named up front (deliverable, not a silent assumption)

Current `WindowKind` enum =
`{MOC_IMBALANCE, OPENING_AUCTION, INDEX_REBALANCE, EARNINGS_DRIFT,
FOMC_BLACKOUT}` — none is a recurring intraday algo-slice checkpoint.
Both cards below require a **versioned taxonomy extension**
`WindowKind.ALGO_CLOCK` (name frozen at implementation; semantic =
recurring RTH institutional slice / checkpoint windows, universe-wide,
`symbol: null`) plus loader/tests. Mis-labeling algo clocks as
`INDEX_REBALANCE` / `MOC_IMBALANCE` is **inadmissible**. This extension
is an implementation deliverable on the selected card's path — not a
research outcome.

---

## H12. ALPHA_ID (proposed): `sig_halfhour_clock_drift_h900_v1`
### (H11′-class reformalization — NEW card)

**HYPOTHESIS.** Institutional execution algos slice parent orders on a
**30-minute wall-clock grid** (10:00, 10:30, … ET) **because** schedule
adherence and participation-rate caps are specified in clock time,
**which must leak into L1 as** elevated signed quote-flow imbalance in
the half-hour-aligned decision windows. Residual permanent impact of
the still-open parent continues over the next **H = 900 s**. The
clock binding is load-bearing: the same flow extreme **off** the
half-hour grid must not produce the same continuation.

Conditional-distribution statement: at each in-window H = 900
boundary (25/session), with `ofi_integrated` = Σ per-event L1 OFI over
the trailing 900 s and `W_hh` = the event is inside a registered
`ALGO_CLOCK` half-hour window (`scheduled_flow_window_active = 1` and
window kind / id in the half-hour set):

`E[mid log-return over next H = 900 s | ofi_integrated percentile ≥ 0.80
(two-sided quintile) AND W_hh AND P(vol_breakout) < 0.7] > 0`
(symmetric short for ≤ 0.20), magnitude κ_frozen × σ₉₀₀ with κ
central ≈ **0.146** ⇒ ≈ **7.0 bps** one-way at the APP median session
(pack-08 σ₉₀₀ med 47.7).

**Why H = 900 (not drafted H11's 1800):** §0.2 — half-hour marks cover
**all** H = 1800 boundaries (frac 1.00), so window-binding F2 is
impossible there. At H = 900, half-hour marks are 12/25 (frac
**0.48**); the complementary 13/25 off-clock boundaries are the F2
contrast set. This is a horizon placement required by falsifiability,
not an economic prior from cycle-1/2 diagnostics (those appear only in
§H12 · OUTCOME-INFORMED PRIORS).

**Why quintile, not decile:** joint fraction
`w_hh × 0.20 = 0.48 × 0.20 = 0.096` fails the ≥ 130 design margin on
{APP, RMBS} even pooled (see block 2). Quintile (0.40) is the minimum
percentile-tail fraction that clears. Occupancy: percentile tail
**exempt**; `w_hh = 0.48` is **grid-geometry identity** (verifiable from
boundary timestamps alone — recorded in §0.2), not a distributional
tape prior. Census still **measures** realized in-window count and
parks on power if the joint falls below the D-C1 / census floors.

**ARCHETYPE & COUNTERPARTY (R2).** Schedule-bound flow-following
(SCHEDULED_FLOW). Actor: clock-sliced institutional parent.
Counterparty: LPs and discretionary flow that under-react to
predictable clock pressure. Conservation: edge ≤ residual impact of
open scheduled parents across the half-hour grid.

**FAMILY & MIRAGE RISK (R3).** `SCHEDULED_FLOW`.
`expected_half_life_seconds = 450`; `horizon_seconds = 900`; ratio
2.0 ∈ [0.5, 4.0] ✓.
`l1_signature_sensors: [scheduled_flow_window]` (G16 rule-5 primary;
`ofi_integrated` is the conditioner, not the family fingerprint).
Mirage: **MIXED (M = 1.5)** — OFI manufacture risk; clock alignment is
structural once calendars are pinned, not exchange-certified.

**OBSERVABLE STATE.** Existing: `scheduled_flow_window` (sensor;
requires calendar injection), `ofi_raw` / `ofi_integrated` at h = 900,
`realized_vol_30s`. Percentile view at h = 900. **No `spread_z_30d`.
No NEW trade-fed sensor.** NEW: `WindowKind.ALGO_CLOCK` + per-session
calendar YAMLs for every evidence-set date (block · CALENDAR).

### H12 · 1. FEASIBILITY (pack-08 §2 / §6; κ FROZEN)

Map κ_req passive H = 900 (med): APP **0.098**, RMBS **0.117** —
both median-open at κ ≤ 0.16. Six others closed at honest κ @900
(pack-08 §2.4) — not deployable.

| factor | central | grounding |
|---|---|---|
| `c_D` | **1.15** | half-hour-aligned quintile (weaker than decile; stronger than unclocked) |
| `f_perm` | 0.52 | scheduled residual permanent share |
| `r_rem` | 0.50 | detection mid-parent |
| `f_H` | 0.75 | 1 − e^(−900/τ), τ = 450/ln 2 |
| `f_pass` | 0.65 | passive same-side pullback haircut |

    κ ∈ [0.03, 0.30], central ≈ 1.15×0.52×0.50×0.75×0.65 ≈ **0.146** — FROZEN

**Park arithmetic (single-stress anchor, pack-08 floors):**

| symbol | κ·σ₉₀₀,med | floor | κ_req med | verdict |
|---|---|---|---|---|
| APP | 0.146 × 47.7 ≈ **6.96** | 4.68 | 0.098 | OPEN — headroom ≈ 1.49× |
| RMBS | 0.146 × 47.3 ≈ **6.91** | 5.51 | 0.117 | OPEN — headroom ≈ 1.25× |

Short-side rider (APP): floor ≈ 2.25 × (2.0 + 0.08 + 0.507) ≈ 5.82 ⇒
κ_req 5.82/47.7 ≈ 0.122 ≤ 0.146 — clears. RMBS short: ≈ 6.60/47.3 ≈
0.140 ≤ 0.146 — clears thinly. Pre-stated: if census-measured short
edge fails rider-inclusive floor on a symbol, that symbol restates
long-only and power is re-checked under the pooled structure — no
threshold tuning.

### H12 · 2. DENSITY WITH MARGIN (actual 140-cell cache; D-C1)

**Basis (09a §2 actuals = pack-08 convention):** 25 × 20 = 500
in-window boundaries/symbol on {APP, RMBS}. HT = 0.90.
`w_hh = 0.48` (§0.2). Gate × warm = 0.90 × 0.95. Quote-fed
conditioner ⇒ contamination-excluded × 1.0 at design. Percentile
quintile **0.40** — occupancy pre-read exempt.

| set | arithmetic | expectation | vs ≥ 130 design / ≥ 100 census |
|---|---|---|---|
| APP alone | 500 × 0.90 × 0.48 × 0.40 × 0.90 × 0.95 | **73.9** | FAIL per-symbol |
| RMBS alone | same | **73.9** | FAIL per-symbol |
| **APP ∪ RMBS pooled** | 1000 × 0.90 × 0.48 × 0.40 × 0.90 × 0.95 | **147.7** | **PASS** design (≥ 130); census floor (≥ 100) cleared with margin |

Design-central = **147.7 ≥ 130**. Conditioning fraction stated:
**0.48 × 0.40**. Decile joint (0.48 × 0.20) → 73.9 pooled — **fails**
design margin; not used. ENSG/MLI not required (see §INGESTION).

### H12 · 3. POWER STRUCTURE + CONSEQUENCE-PRECEDENCE (freeze-ready;
failure modes per backlog 17)

| role | symbols | basis |
|---|---|---|
| **Deployable (D)** | {APP, RMBS} | both median-open at κ_frozen @900 |
| **Evidence-only** | — | none; six others closed at honest κ @900 |
| **Evidence structure** | **pooled** APP ∪ RMBS | primary RankIC / power on the pool; per-symbol diagnostic only |

**Consequence-precedence sketch (copy into protocol freeze before any
instrument runs):**

1. Primary §9 gate rows outrank safeguards on the same statistic
   (safeguard may tighten a pass, never loosen a primary fail).
2. Pooled power bar governs census PROCEED/PARK: design headline ≥ 130;
   census park floor ≥ **100** (D-C1). A single-symbol shortfall inside
   the pool does **not** park if the pool clears — unless that symbol
   also fails deployability park arithmetic, in which case it drops
   from D and the pool is re-checked vs ≥ 100.
3. Magnitude-class IC bars (when frozen) are `n-invariant` →
   REJECTED-terminal; power-class census misses → PARK
   evidence-infrastructure only when the freeze says so.
4. **F2 window-binding** (mechanism): fail → REJECTED (substance),
   regardless of RankIC magnitude/sign. Label at freeze: mechanism /
   n-invariant for the binding claim (not curable by more off-clock
   silence alone if the in-window arm also fails magnitude — both
   reported).
5. Undefined intersection = freeze-blocking defect — no post-outcome
   adjudication.

**Contingent-trigger failure modes** (if Lei holds H13 contingent on
H12 — backlog 17; enumerate now even if sequencing is “H12 only”):

| H12 step-2b mode | H13 posture (if contingent) |
|---|---|
| magnitude FAIL (`n-invariant`) | presumptive death (shared scheduled-flow continuation magnitude regime) |
| significance FAIL only (`n-variant`) | not automatic death; Lei re-auth with honest-N; H13 may still proceed as distinct hour×1800 claim |
| F2 window-binding FAIL | presumptive death for any card sharing the same `ALGO_CLOCK` binding architecture |
| form / G16 / calendar FAIL | H13 not authorized until form path is clean (shared taxonomy deliverable) |
| power PARK (census < 100) | H13 not authorized from H12's park; H13 may be re-sequenced only with its own census on its own D |

### H12 · 4. WARM REALITY (measured-or-pre-registered)

| sensor | status | basis |
|---|---|---|
| `scheduled_flow_window` | **calendar-warm pre-registered** | Sensor `warm=False` when the injected calendar has no symbol-eligible windows (`scheduled_flow_window.py`). For every (symbol ∈ D, session ∈ operative 20), a calendar YAML **must** exist containing the half-hour `ALGO_CLOCK` set; missing/empty calendar → warm=False → entry suppressed (Inv-11). Census reports fraction of boundaries with calendar-warm ∧ quote path live; warm < 0.5 on > 2 sessions drops that symbol from D. |
| `ofi_integrated` @900 | asserted from quote rates; census verification | 03c §5.1 APP 1.6–3.0 quotes/s ⇒ 900 s window ample; same drop rule |
| `realized_vol_30s_zscore` | **measured** 0.94–0.995 (H2 C.5) | gate — safe |
| `spread_z_30d` | — | **NOT USED** |

### H12 · 5. CONTAMINATION POSTURE (03b)

Entry conditioner is **quote-fed** (`ofi_integrated`) × **calendar
membership** (no tape). Class-B prints never enter `ofi_raw`. No NEW
trade-fed extreme conditioner. DI-09 extreme-print finding does not
apply to entry.

**REPORTS estimands (backlog 18) — labeled at design for freeze:**

| field (proposed) | estimand | expected |
|---|---|---|
| `off_clock_cotravel_rate` | share of quintile-OFI boundaries that are off-clock (geometry diagnostic) | ≈ 0.52 by §0.2 — **not** leakage |
| `calendar_missing_rate` | boundaries with sensor warm=False due to missing calendar | **0** by construction after artifacts land; > 0 → infrastructure FAIL |
| (no `residual_non_a_share`) | — | N/A — no Class-A trade filter on entry |

### H12 · 6. REJECTED-CLAIM ADJACENCY

| claim | relation |
|---|---|
| (a) Drafted H11 `sig_halfhour_clock_drift_v1` | **Reformalization of that mechanism class**, not a patch: adds `scheduled_flow_window` + `W_hh` predicate + calendar deliverables; moves H 1800 → 900 so F2 is non-vacuous. N-impact 0 until outcome contact. |
| (b) H10 extreme-SFI KYLE @900 | **Not that claim.** Different family; no ISO/SFI conditioner; F2 is window-binding, not λ/volume co-travel. |
| (c) H8 elevated-λ continuation @300 | **Not that claim.** Different family/horizon/conditioner. |
| (d) H4 close-rebalance | **Not that claim.** Not MOC/close-window-specific; not taker. H4 park does not adjudicate this card (pack-10 DISPOSITION 1). |

**FAILURE MODES (≥3).** (a) Clock crowding → zero residual (dilution).
(b) Quintile + window joint too weak — c_D prior fails, F1 dead.
(c) OFI manufacture on a known clock (adversarial) — negative tail.
(d) Calendar/taxonomy defect → warm starvation or wrong windows
(infrastructure, not edge). (e) Soft confusion with unclocked OFI
continuation — blocked by F2.

**FALSIFICATION.** F1: RankIC on pre-registered pooled in-window
quintile boundaries ≤ 0 or below honest-N ceiling. **F2 (mandatory
window-binding):** matched ofi-quintile conditioning on **off-clock**
boundaries (`W_hh = 0`) shows continuation of the same sign/magnitude
class → claim refuted (clock decoration, not scheduled flow). F3:
regime/stratum sign flip. F4: passive realism / `--inv12-stress` →
trap-quadrant. F5: structural boundaries §0.1.

**CALENDAR ARTIFACTS (named deliverables).**

1. `WindowKind.ALGO_CLOCK` enum + loader/tests (versioned migration;
   `EventCalendar.hash` baseline refresh).
2. Per-session YAML for every date in the operative {APP, RMBS}
   20-session sets (03c A1.1 ∪ original 10), each containing universe-wide
   half-hour `ALGO_CLOCK` windows covering the twelve §0.2 marks
   (interval convention frozen at impl: half-open windows containing
   the decision timestamp, e.g. `[mark, mark+ε)` or
   `[mark−lead, mark+ε)` with lead stated in the formal spec — **must**
   make `contains(boundary_ts)` true iff the boundary is a half-hour
   mark).
3. Provenance: each file content-addressed; bootstrap calendar hash in
   run provenance (Inv-13).

**IMPLEMENTATION.** Taxonomy + calendars + YAML + percentile factory
at h = 900. No NEW trade-fed sensor. Ordering-B eligible (backlog 14)
at Lei's option — not mandated by this slate.

### H12 · OUTCOME-INFORMED PRIORS (disclosure — pack-10 DISPOSITION 3)

| prior | source | use on this card | status |
|---|---|---|---|
| H = 300 → H = 900 RankIC magnitude (0.0186 → 0.0893) | H8/H10 POST-HOC shelf | **Horizon selection discussion only** — supported placing the reformalized claim at H = 900 rather than forcing a vacuous H = 1800 half-hour design; **never** used to set κ, RankIC bars, or as proof | disclosed; not evidence |
| H10 F2 KYLE miss at H = 900 | H10 S.8 | Motivates family switch to SCHEDULED_FLOW (authorized); **not** a magnitude prior for this card | disclosed |
| Cycle-1/2 warm / ISO / occupancy diagnostics | H2/H8/H10 censuses | Conventions only (no `spread_z_30d`; percentile tails; D-C1) | disclosed |

---

## H13. ALPHA_ID (proposed): `sig_hour_checkpoint_drift_h1800_v1`

**HYPOTHESIS.** Larger institutional parents and desk-level participation
caps are often monitored on an **hourly** checkpoint grid (10:00,
11:00, … ET) **because** schedule variance and VWAP benchmarks are
reviewed on the hour, **which must leak into L1 as** signed quote-flow
pressure concentrated on on-the-hour decision windows. Residual impact
of still-open parents continues over the next **H = 1800 s**. Binding
claim: matched flow extremes at **half-hour-but-not-hour** marks
(:30) must not produce the same continuation — otherwise the hour
checkpoint story collapses into generic half-hour / unclocked flow.

Conditional-distribution statement: at each in-window H = 1800
boundary (12/session), with `ofi_integrated` over trailing 1800 s and
`W_hr` = inside a registered `ALGO_CLOCK` **on-the-hour** window:

`E[mid log-return over next H = 1800 s | ofi_integrated percentile ≥ 0.80
AND W_hr AND P(vol_breakout) < 0.7] > 0` (symmetric short),
magnitude κ_frozen × σ₁₈₀₀ with κ central ≈ **0.160** ⇒ ≈ **10.0 bps**
at APP median (0.160 × 62.8).

**Why not “H12 at 1800”:** (i) half-hour windows are tautological at
H = 1800 (§0.2); (ii) hour-only windows give frac **0.50** and a
non-empty F2 contrast (:30 marks); (iii) longer residual half-life and
multi-symbol D are part of the claim, not a decoration.

**ARCHETYPE & COUNTERPARTY (R2).** Schedule-bound flow-following;
actor = hour-checkpointed institutional parent / desk; counterparty =
under-reacting LPs between hourly reviews. Conservation: edge ≤
residual impact across the hourly grid.

**FAMILY & MIRAGE RISK (R3).** `SCHEDULED_FLOW`.
`expected_half_life_seconds = 900`; `horizon_seconds = 1800`; ratio
2.0 ✓. `l1_signature_sensors: [scheduled_flow_window]`. Mirage:
**MIXED (M = 1.5)**. Soft-σ caveat: σ₁₈₀₀ rests on 12 returns/session
(pack-08 §1 / pack-05 caveat 2).

**OBSERVABLE STATE.** Same stack as H12 at h = 1800; hour-only
`ALGO_CLOCK` calendars (subset of marks). No NEW trade-fed sensor.

### H13 · 1. FEASIBILITY (pack-08; κ FROZEN)

Map κ_req passive H = 1800 (med): APP **0.074**, RMBS **0.086**,
OLN **0.153**, DIOD **0.131**, PCTY **0.140**, CROX **0.136** —
median-open at κ ≤ 0.16. ENSG **0.169** / MLI **0.187** — **closed**
at median honest κ (pack-08 §2.4).

| factor | central | grounding |
|---|---|---|
| `c_D` | **1.20** | on-the-hour subset (stricter clock than half-hour) |
| `f_perm` | 0.55 | longer residual permanent share |
| `r_rem` | 0.55 | parent still open across next half-hour+ |
| `f_H` | 0.80 | 1 − e^(−1800/τ), τ = 900/ln 2 |
| `f_pass` | 0.65 | passive haircut |

    κ central ≈ 1.20×0.55×0.55×0.80×0.65 ≈ **0.172** — FROZEN
    (clears OLN κ_req 0.153; ratchet-down only)

**Park arithmetic:**

| symbol | κ·σ₁₈₀₀,med | floor | κ_req med | verdict |
|---|---|---|---|---|
| APP | 0.172 × 62.8 ≈ **10.8** | 4.68 | 0.074 | OPEN |
| RMBS | 0.172 × 63.8 ≈ **11.0** | 5.51 | 0.086 | OPEN |
| OLN | 0.172 × 56.8 ≈ **9.77** | 8.69 | 0.153 | OPEN — headroom ≈ 1.12× (thin; 10-session σ) |
| DIOD | 0.172 × 47.4 ≈ **8.15** | 6.23 | 0.131 | OPEN |
| PCTY | 0.172 × 37.2 ≈ **6.40** | 5.19 | 0.140 | OPEN — thin |
| CROX | 0.172 × 41.5 ≈ **7.14** | 5.66 | 0.136 | OPEN |
| ENSG, MLI | — | — | 0.169 / 0.187 | **CLOSED** at median — not in D |

Short riders: APP/RMBS clear with margin at H = 1800 (κ_req ~0.09–0.10).
OLN/PCTY thin — same long-only restatement chain as H12 if rider fails.

**Tranche-1B κ caveat (explicit):** park numbers for
{OLN, DIOD, PCTY, CROX} use pack-05/08 **10-session** σ. If a
census-legal σ refresh on the operative 20 raises median κ_req above
0.172 for a symbol, that symbol **drops from D** and the pool is
re-checked (block 3) — no κ inflation.

### H13 · 2. DENSITY WITH MARGIN (actual 140-cell cache; D-C1)

Per-symbol raw H = 1800 at 20 sessions: **240** (09a §2).
`w_hr = 0.50` (§0.2). HT = 0.90. Quintile 0.40. gw = 0.90 × 0.95.

| set | arithmetic | expectation | vs ≥ 130 / ≥ 100 |
|---|---|---|---|
| each of {APP, RMBS, OLN, DIOD, PCTY, CROX} alone | 240 × 0.90 × 0.50 × 0.40 × 0.90 × 0.95 | **36.9** | FAIL per-symbol (required) |
| APP ∪ RMBS only | 480 × … | **73.9** | **FAIL** design margin |
| **D = six-symbol pool** | 1440 × 0.90 × 0.50 × 0.40 × 0.90 × 0.95 | **221.6** | **PASS** (≥ 130); census floor cleared |

Design-central = **221.6 ≥ 130**. Conditioning fraction stated:
**0.50 × 0.40**. Pooled structure **mandatory** — and the pool must
include Tranche-1B four (κ-open at 1800) or the card fails density on
{APP, RMBS} alone.

### H13 · 3. POWER STRUCTURE + CONSEQUENCE-PRECEDENCE (freeze-ready;
failure modes)

| role | symbols |
|---|---|
| **Deployable (D)** | {APP, RMBS, OLN, DIOD, PCTY, CROX} — all median-open at κ_frozen @1800 on committed map |
| **Evidence-only** | — |
| **Excluded** | {ENSG, MLI} — median-closed at honest κ; DRAWN-NOT-INGESTED expansion cells not requested (see §INGESTION) |
| **Evidence structure** | **pooled only** across D; per-symbol cannot PROCEED |

**Consequence-precedence sketch:** H12 defaults, plus: (i) treating
per-symbol n ≥ 130 as a PROCEED path is a **freeze-blocking defect**;
(ii) symbol drop from D for κ/warm/rider → re-check pool vs ≥ 100;
if pool fails → PARK; (iii) F2 = continuation at matched ofi quintile
on `:30` (half-hour-not-hour) boundaries refutes; (iv) primary §9 >
safeguards; (v) soft-σ₁₈₀₀ sampling error is a disclosed feasibility
caveat — not a post-hoc κ rescue.

**Contingent-trigger failure modes** (if H13 is held contingent on
H12 — backlog 17):

| H12 mode | H13 posture |
|---|---|
| magnitude FAIL | presumptive death (shared continuation-magnitude regime on this universe) |
| significance FAIL only | not automatic death — distinct hour×1800×six-symbol claim may proceed under Lei re-auth + honest-N |
| F2 half-hour window-binding FAIL | **does not** auto-kill H13 if H13's F2 is hour-vs-:30 (different binding); Lei still requires extraordinary justification if the kill suggests OFI×clock is generally null |
| form/calendar FAIL on shared `ALGO_CLOCK` | H13 blocked until taxonomy/calendars land |
| power PARK | H13 may still be censused independently (different D / H); not authorized as a free retry of H12's frozen pool |

### H13 · 4. WARM REALITY

| sensor | status | basis |
|---|---|---|
| `scheduled_flow_window` | **calendar-warm pre-registered** | Same semantics as H12; every (symbol ∈ D, session ∈ that symbol's operative 20) needs hour-only `ALGO_CLOCK` YAML. Thin names (DIOD/PCTY) quote-warm for `ofi_integrated` @1800 asserted — census drop rule identical. |
| `ofi_integrated` @1800 | asserted; census verification | quote-fed |
| `realized_vol_30s_zscore` | measured — safe | gate |
| `spread_z_30d` | **NOT USED** | — |

### H13 · 5. CONTAMINATION POSTURE (03b)

Identical class to H12 (quote-fed × calendar). REPORTS estimands:

| field (proposed) | estimand | expected |
|---|---|---|
| `halfhour_not_hour_cotravel_rate` | share of quintile-OFI H = 1800 boundaries that are :30 (F2 geometry) | ≈ 0.50 — **not** leakage |
| `calendar_missing_rate` | as H12 | **0** after artifacts |
| `tranche1b_kappa_drift` | census-measured median κ_req − design κ_req on {OLN, DIOD, PCTY, CROX} | diagnostic; drop-from-D if κ_req > κ_frozen |

### H13 · 6. REJECTED-CLAIM ADJACENCY

| claim | relation |
|---|---|
| (a) H12 half-hour @900 | **Related family, distinct claim:** hour-only windows, H = 1800, six-symbol D, different F2 contrast (:30 vs off-15). Not a parameter variant of H12. |
| (b) Drafted H11 | Same reformalization family; H13 is the H = 1800 path that survives §0.2 by restricting to hour marks. |
| (c) H4 MOC/close | **Not that claim.** Not `MOC_IMBALANCE`; not close-window-only; not taker. |
| (d) H10 KYLE @900 | **Not that claim.** |

**FAILURE MODES.** (a) Hour crowding / zero residual. (b) Soft σ₁₈₀₀
mis-states κ_req (dilution of feasibility). (c) OLN/PCTY thin park →
pool power collapse after D-drop. (d) Tranche-1B κ drift after σ
refresh. (e) F2 fail (:30 matches hour) → hour story false.

**FALSIFICATION.** F1 as usual on in-hour pooled boundaries.
**F2:** matched ofi-quintile on `:30` boundaries (`W_hr = 0`) shows
same continuation class → refuted. F3–F5 as H12.

**CALENDAR ARTIFACTS.** Same `ALGO_CLOCK` taxonomy as H12; YAML set
covers the **union of operative dates across all six D symbols**
(original 10 + A1.1 expansion 10); each file carries **on-the-hour
only** windows (six marks/session per §0.2). Shared taxonomy with
H12; window **subset** differs by card.

**IMPLEMENTATION.** Calendars + YAML + percentile at h = 1800. No NEW
trade-fed sensor. Larger calendar surface than H12 (six symbols × 20).

### H13 · OUTCOME-INFORMED PRIORS (disclosure)

| prior | use | status |
|---|---|---|
| H = 300 → H = 900 magnitude lift | May **bias** toward longer horizons in ranking discussion; **must not** set κ or bars; soft-σ₁₈₀₀ cuts the other way | disclosed; not evidence |
| H10 F2 miss | Family switch only | disclosed |
| H12 geometry lesson (§0.2) | Forced hour-only subset at 1800 — design constraint, not an outcome statistic | disclosed |

---

## § INGESTION — ENSG / MLI

**Not requested for either card.**

| card | need? | arithmetic |
|---|---|---|
| H12 | No | Pooled design-central **147.7 ≥ 130** on {APP, RMBS} alone; ENSG/MLI closed at honest κ @900 anyway (pack-08 §2.4) — ingestion cannot open them at median κ (A2.4 / H8 n-invariant lesson) |
| H13 | No | Six-symbol pool **221.6 ≥ 130**; ENSG/MLI remain median-closed at H = 1800 honest κ (0.169 / 0.187) — would be evidence-only at best; A2.4 deferral stands |

If a future D-drop cascade on H13 leaves pool design-central < 130,
revisit ingestion **only** as a Lei grid-amendment (backlog 16) with
fresh arithmetic — not pre-authorized here.

---

## Constraint compliance check (slate level)

- Exactly two SCHEDULED_FLOW candidates ✓
- Passive; H ∈ {900, 1800} ✓
- Both carry `scheduled_flow_window` + window predicate + named
  calendar deliverables ✓
- F2 window-binding mandatory on both; H = 1800 avoids tautological
  half-hour set ✓
- Density recomputed on actual 140-cell basis (09a §2) with HT +
  D-C1 ≥ 130 vs census ≥ 100 ✓
- Power structure + consequence-precedence + failure **modes**
  (backlog 17) ✓
- Warm incl. calendar-warm ✓
- Contamination + REPORTS estimands (backlog 18) ✓
- Outcome-informed priors in dedicated blocks ✓
- ENSG/MLI ingestion arithmetic; not requested ✓
- No IC / forward return / signal evaluation in producing this file ✓
- N = 12 unchanged ✓

---

## (1) Ranking

### (1a) Hard pre-filters FIRST (cost-floor AND density-margin)

| card | family × H × mode × set | κ_frozen | κ_req (med, set) | density (design-central) | cost | density | enter? |
|---|---|---|---|---|---|---|---|
| H12 | SCHED × 900 × pas × {APP,RMBS} pool | 0.146 | 0.098 / 0.117 | **147.7** | PASS | PASS | **YES** |
| H13 | SCHED × 1800 × pas × six-symbol pool | 0.172 | 0.074–0.153 | **221.6** | PASS | PASS | **YES** |

Form pre-filter (G16 / reformalization bar): both drafted to **PASS**
pending taxonomy+calendar landing (deliverables named; not residue).

### (1b) S × F ÷ M on survivors — blind spots named first

Blind spots (pack-07 item 2 / cycle-2 record): formula cannot see
(i) short-side × power interactions, (ii) cost-floor contact disguised
by illustrative edges, (iii) design-vs-realized occupancy when
non-geometry priors sneak in, (iv) **soft σ₁₈₀₀** (H13), (v) **Tranche-1B
κ drift** on four symbols without map refresh (H13), (vi) stop-rule
asymmetry — a cheap form/calendar fail closes the **entire program**
under pack-10 DISPOSITION 2. Cold-read remains load-bearing.

| # | candidate | S | F | M | S×F÷M | notes |
|---|---|---|---|---|---|---|
| H12 | `sig_halfhour_clock_drift_h900_v1` | 4 | 4 | 1.5 | **10.7** | H11′ bar met; F2 contrast natural at H = 900; D = {APP,RMBS} only; calendar surface smaller; quintile weakens c_D |
| H13 | `sig_hour_checkpoint_drift_h1800_v1` | 4 | 3 | 1.5 | **8.0** | Stronger density margin; F lower (six-symbol calendars + soft σ + κ-drift risk); hour binding is the clean 1800 path |

Formula ranking: **H12 > H13.**

---

## (2) Recommendation — ONE candidate

**H12, `sig_halfhour_clock_drift_h900_v1` (H11′-class).**

Rationale (pre-registered, not hidden): among the two survivors, H12
is the only card that (a) discharges the H11 reformalization bar with a
**non-vacuous** F2 contrast on the decision grid without requiring
Tranche-1B κ assumptions, (b) clears D-C1 on the already-outcome-contacted
{APP, RMBS} session counts without six-symbol calendar expansion, and
(c) minimizes stop-rule ruin from infrastructure (smaller calendar
surface; no thin OLN/PCTY park cascade). H13 remains the structural
runner-up / diversification path at H = 1800 with mandatory pooling.

Stated for review: if Lei weighs raw density margin and longer-horizon
residual claim over F2 cleanliness and κ-refresh risk, overriding to
H13 is defensible; the slate recommends H12. Holding H13 as
**CONTINGENT SECOND CARD** with the failure-mode table in H12 · 3 /
H13 · 3 is available — not selected here.

---

## (3) TRIAL-COUNT LEDGER — appendix (N = 12 UNCHANGED; drafted rows N-neutral)

FQ-6B-R: data contact increments N; drafting does not. Living N = 12
(pack-10 §6 / DISPOSITION 5). Every row below is
**drafted-not-evaluated (N-impact: 0)** until outcome contact; any
future DSR uses the then-current living N (≥ 12). First outcome
contact on any primary below is **+1 → N ≥ 13**.

| N | trial | source | status |
|---|---|---|---|
| — | H12 primary: ofi_integrated(900 s) quintile × half-hour `ALGO_CLOCK` continuation, H=900, hl=450, passive, pooled {APP,RMBS} | H12 | drafted-not-evaluated (N-impact: 0) |
| — | H12 alt: decile tail instead of quintile (power-infeasible on current arithmetic — recorded only) | H12 | drafted-not-evaluated (N-impact: 0); not authorized |
| — | H13 primary: ofi_integrated(1800 s) quintile × hour `ALGO_CLOCK` continuation, H=1800, hl=900, passive, pooled six-symbol D | H13 | drafted-not-evaluated (N-impact: 0) |
| — | H13 alt: drop OLN from D at freeze (thicker headroom; re-check pool ≥ 130) | H13 | drafted-not-evaluated (N-impact: 0) |
| — | Shared: `WindowKind.ALGO_CLOCK` taxonomy + calendar artifact set | H12/H13 | infrastructure; N-impact 0 until paired with an outcome contact |

Carried unchanged (not duplicated): pack-04 / pack-06 / pack-09
drafted rows and closed trials (H1–H11, S-1). None authorized for
evaluation by this slate. H9 remains dead pending extraordinary
justification (pack-09 DISPOSITIONS 8). KYLE_INFO continuation not
authorized (pack-10 DISPOSITION 1).

**N = 12 as of this task** — two cards drafted, zero outcome
statistics for any slate-D candidate.

---

**Task 6-D drafting complete (2026-07-16). Cold-read dossier:
`prompt_pack_11a_slate_d_review.md`. Selection: see DISPOSITIONS
below.**

---

## DISPOSITIONS (Slate D final selection, 2026-07-16 — Lei; append-only, cards above unedited)

1. **H12 CONFIRMED.** `sig_halfhour_clock_drift_h900_v1` is the
   selected candidate. Governing order is the formula ranking
   (§1b), the independent cold-read order
   (`prompt_pack_11a_slate_d_review.md` §7), and the slate
   recommendation (§2): **H12 > H13.** Full review record:
   `prompt_pack_11a_slate_d_review.md` DECISION RECORD.

2. **H13 HELD CONTINGENT** — not selected as co-primary, not parked,
   and not authorized for its own census yet. Pre-registered
   activation triggers (exclusive; backlog 17 mode table frozen at
   these three):

   | # | H12 mode | H13 posture |
   |---|---|---|
   | **(a)** | H12 **design/census death** | **ACTIVATES.** κ remains **frozen at 0.172** (minimum rule vs factor product 0.189 — arithmetic bug logged in 11a §2 H13 check d / Q1; factors are not restated to erase the freeze). Pool-collapse floor behavior (axis-split on D-drop: 6→221.6 … 3→110.8 clears census ≥100 / fails design ≥130; 2→73.9 fails both — 11a §2e) **must be frozen into the protocol before any H13 census**. |
   | **(b)** | H12 step-**2b fail** with **F2-binding PASS** | **ACTIVATES.** Sibling arithmetic disclosed: H12's clock-bound substance held; magnitude/significance kill does not auto-kill the distinct hour×1800×six-symbol claim. Honest-N entry as H13's own trial at first outcome contact. |
   | **(c)** | H12 step-**2b fail** with **F2-binding NEGATIVE** | **Activation only after Lei reviews** the F2 decomposition by window type (hour-subset masking vs half-hour-off-clock). Disclosed: that review is **outcome-contaminated** relative to H12's contacted grid — extraordinary bar applies; not automatic activation. |

   Cards above stay as pre-registered (FQ-6B-R: drafting is N-impact
   0). Absent a trigger firing, H13 is not sequenced.

3. **H12 ↔ H9 FIREWALL (both directions — binding).**

   - **H12 → H9:** H12 evidence **never cites toward H9 revival**.
     An H12 **F2-pass** strengthens H9's presumptive death (clock-bound
     OFI is not KYLE rehabilitation of unclocked OFI continuation;
     pack-09 DISPOSITIONS 8 bar stands). An H12 **F2-fail**
     out-window arm that shows continuation is a **contaminated shelf**
     — not extraordinary justification for H9, not a KYLE attribution
     restore, and not reusable as confirmation evidence for H9.
   - **H9 → H12:** H9 history (dead claim, untested OFI KYLE @900,
     adjacency prose) **never prejudices H12 scoring** — κ, RankIC
     bars, census floors, F1/F2 adjudication, or stop-rule accounting.
     H12 is evaluated fresh against its own gates (session constraint
     6).

4. **Stop-rule restated (pack-10 DISPOSITION 2).** Either card's
   step-**2b PASS** satisfies the cycle-3 stop-rule (program continues
   on that survivor). If both cards are exhausted without a 2b PASS
   (H12 dies and H13 never activates, or H13 activates and then fails
   2b), the program **closes** per pack-10. Kill-gate class does not
   matter for closure.

5. **N = 12 unchanged** — no data contact in this adjudication; no
   outcome statistic exists for any slate-D candidate. First outcome
   contact on the H12 primary remains **+1 → N ≥ 13** per §(3).
   Any future DSR uses the then-current living N (≥ 12).
