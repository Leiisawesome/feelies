"""Acceptance tests for the architecture contract and status matrix.

These tests intentionally overlap lower-level suites so missing coverage
breaks the corresponding acceptance row. Slow checks use ``pytest.mark.slow``.
"""
