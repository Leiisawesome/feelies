---
description: Core platform invariants for the intraday trading system. These 13 rules are the structural contract across all skills — every component, signal, and deployment must satisfy them.
alwaysApply: true
---

# Platform Invariants

## Epistemological

1. **Structural mechanism required** — every signal and entry must name the causal force being exploited. No mechanism, no trade.

2. **Falsifiability before testing** — define what disproves the hypothesis before looking at data. No retrospective narratives.

3. **Evidence over intuition** — scaling, quarantine, optimization, and promotion decisions require statistical evidence. Measure first, act second.

4. **Decay is the default** — every edge is assumed to be eroding. Burden of proof is on continued viability.

## Architectural

5. **Deterministic replay** — same event log + parameters → bit-identical signals, orders, PnL. No exceptions.

6. **Causality enforced** — features/signals at time T use only events with timestamp ≤ T. Processing delay modeled explicitly. No lookahead.

7. **Event-driven, typed schemas** — all data flows through typed events on the bus. No polling. No untyped messages crossing layer boundaries.

8. **Layer separation** — every piece of code belongs to exactly one layer. No cross-layer leakage. No hidden global state.

9. **Backtest/live parity** — shared core logic; mode-specific code only behind `ExecutionBackend`. Divergence is a flaggable defect.

10. **Clock abstraction** — all timestamps via injectable clock. No raw `datetime.now()` in core logic.

## Safety

11. **Fail-safe default** — unknown states, missing data, and errors resolve to reduced exposure, never increased. Safety controls only tighten autonomously; loosening requires human re-authorization.

12. **Transaction cost realism** — `expected_edge > 1.5× round_trip_cost`. Must remain viable under 1.5× costs and 2× fill latency (`--inv12-stress`). If alpha vanishes under stress, it wasn't real.

## Provenance

13. **Full provenance, versioned and auditable** — every decision traceable to an event. Every feature to a version. Every strategy to a hypothesis. Every config change to an author and rollback path. Nothing reaches capital without a reproducible chain.

## Glossary

Canonical term meanings — no local redefinition in skills. Operational detail: `.cursor/skills/` (see [README](../skills/README.md)).

| Term | Definition |
|------|------------|
| **strategy** | Deployable, versioned bundle of signal logic, sensors, risk params, and config. Atomic unit of promotion, quarantine, and capital allocation. |
| **signal** | Typed `Signal` event: direction, strength, edge estimate. Stateless — no position or order awareness. See microstructure-alpha skill. |
| **sensor** | Layer-1 incremental observer over L1 events; emits versioned `SensorReading` via `SensorRegistry`. See feature-engine skill. |
| **feature** | Canonical type: `HorizonFeatureSnapshot` on `HorizonTick` boundaries. D.2 retired per-tick `FeatureVector`. See feature-engine skill. |
| **horizon signal** | `HorizonSignal.evaluate` after `regime_gate` is ON; one `Signal` per (alpha, symbol, boundary). See microstructure-alpha skill. |
| **regime gate** | AST-safe boolean DSL (`on_condition` / `off_condition`) on SIGNAL alphas. See microstructure-alpha skill. |
| **cost arithmetic** | Required SIGNAL YAML block; `margin_ratio ≥ 1.5` and ±0.05-absolute reconciliation (`alpha/cost_arithmetic.py`; Inv-12). Metadata only — does not size at runtime. |
| **layer gates (G1–G17)** | `LayerValidator` calls G1–G17 before load. G1/G3 may downgrade via `enforce_layer_gates`; G13 is presently a no-op; all other implemented checks are blocking when applicable. See `alphas/SCHEMA.md`. |
| **trend mechanism** | Closed enum declaring the causal force (Inv-1); validated by G16. Propagated on `Signal` and intent breakdown. |
| **hazard spike** | `RegimeHazardSpike` when dominant regime posterior drops across consecutive ticks. Exit-direction hint only (Inv-11). See regime-detection skill. |
| **expected half-life** | `expected_half_life_seconds` on mechanism block and `Signal`; G16 envelope + horizon ratio `[0.5, 4.0]`. |
| **horizon** | Seconds bucket for aggregation; canonical set `{30, 120, 300, 900, 1800}`. |
| **edge** | Structural phenomenon exploited; exists independent of trading. Assumed to decay (Inv-4). |
| **alpha** | Measured residual return after costs; evidence that an edge pays. Distinct from edge (overfitting vs real mechanism). |
| **replay** | Re-feed events in time order. Deterministic replay (Inv-5): identical inputs → identical outputs. |
| **backtest** | Replay plus fill, cost, and latency models → simulated PnL. |
| **simulation** | Any non-live mode (backtest, paper, research, stress). |
| **regime** | Classifiable microstructure state. Writer: regime-detection; consumers: risk, gates, forensics. |
| **warm-up** | Insufficient history → `warm=False`; suppress entries, not exits. |
| **staleness** | Missing fresh data → `stale=True`; suppress entries, exits allowed. |
| **quarantine** | LIVE strategy removed from capital; paper signals continue until revalidation. |
| **promotion** | Gated RESEARCH → PAPER → LIVE path with structured evidence. See alpha-lifecycle skill; acceptance bars in testing-validation skill. |
| **promotion ledger** | Append-only JSONL of lifecycle transitions (`promotion_ledger.py`). Never read on the tick path (forensic only). |
| **operator CLI** | `feelies` entry point (`cli/`). `feelies promote` (alpha-lifecycle), `feelies backtest` (backtest-engine). |
| **gate matrix** | `GATE_EVIDENCE_REQUIREMENTS` maps each `GateId` to evidence types. See alpha-lifecycle skill. |
| **CPCV evidence** | Cross-validation evidence for PAPER→LIVE gate. Schema in `promotion_evidence.py`. |
| **DSR evidence** | Deflated Sharpe excess for PAPER→LIVE. Platform `dsr` is Sharpe units, not canonical BLP probability. |
| **paper-window evidence** | Sim-vs-live divergence fields for PAPER→LIVE. Thresholds in testing-validation skill. |
| **capital-stage tier** | `SMALL_CAPITAL` or `SCALED` on LIVE alphas; LIVE→LIVE self-loop for escalation. See alpha-lifecycle skill. |
| **quarantine-trigger evidence** | Forensic payload for LIVE→QUARANTINED; demotion always commits (Inv-11). See post-trade-forensics skill. |
| **revalidation evidence** | Required to leave QUARANTINED for PAPER; includes non-empty `human_signoff`. |
| **portfolio alpha** | `layer: PORTFOLIO`; cross-sectional weights → `SizedPositionIntent`. See composition-layer skill. |
| **cross-sectional context** | Fan-in of universe `Signal`s per boundary; `completeness` vs `composition_completeness_threshold` (default 0.80). |
| **sized position intent** | Layer-3 desired book state; `check_sized_intent` → per-leg orders with per-leg veto (Inv-11). |
| **hazard exit** | `OrderRequest` from `HazardExitController` on spike or max age; `reason ∈ {HAZARD_SPIKE, HARD_EXIT_AGE}`. |
| **trend mechanism breakdown** | Per-family gross share on `SizedPositionIntent`; input to forensics and G16 caps. |
| **decay weighting** | Optional ranker weight `exp(-age/hl)`; separate L3 parity hash when enabled. |
| **mechanism concentration** | Realised single-family gross share on an intent; capped at load (G16) and ranker. |
| **enforce_layer_gates** | If true, G1/G3 block loads; if false, only G1/G3 downgrade to warnings. G13 is presently a no-op; G2, G4–G12, and G14–G17 remain blocking when applicable. |
| **strict typing** | `mypy --strict` on all `src/feelies/`; no `ignore_errors` overrides (acceptance-locked). |
