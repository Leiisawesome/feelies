# Audit 2026-04-17 — d05ac68 + 3 uncommitted fixes (A-DET-02, A-TEST-02, B-PROMO-04)

Protocol version: v1.0
Config checksum: `cf9b8c319e8f687f7a64559378ec5641a95d78703ff4934ee83c7abf89265f34` (bit-identical across two consecutive `--demo` runs — see A-DET-02 below)
Artifact id: `e247a17b47adf15b3481087b1d3090f906e0e199c7f4a86e2c2528e03fbd49f9` (`engine_version=0.1.0`, `data_version=demo:06ae79f1ff97680a`, strategy=`trade_cluster_drift@1.0.0`)
Tests: passed=1042 failed=0 skipped=3 xfailed=0 (vs prior: +2 passed)
Demo backtest parity hash: `390f2f4ea1f80905a354949311877cb1fb7d84a5f4d6a5d989a262e7503494f6` (matches prior: no — prior baseline drifted; this iteration is now stable)
Demo run twice — parity hashes: `390f2f4ea1f80905a354949311877cb1fb7d84a5f4d6a5d989a262e7503494f6` | `390f2f4ea1f80905a354949311877cb1fb7d84a5f4d6a5d989a262e7503494f6` (bit-identical: yes)
Stress 1.5x net PnL: $0.00 (0 trades on the 8-tick demo; pipeline executed end-to-end)  Stress 2.0x net PnL: $0.00 (idem)
tick_to_decision_latency_ns p50/p95/p99: 40000 / 88000 / 88000 (i.e. 0.040 / 0.088 / 0.088 ms — well below the 10 ms hard ceiling and the 3 ms target)
Iteration verdict: PASS

## Pillar A — Structural

### A1. Layer separation & hidden state
- A-LAYER-01: PASS — unchanged vs baseline. No new cross-layer imports introduced by the three fixes (changes confined to `src/feelies/core/platform_config.py:_to_dict`, `scripts/run_backtest.py`, `tests/validation/test_determinism_props.py`).
- A-LAYER-02: PASS — module-level constants in `kernel/`, `risk/`, `execution/`, `features/` remain `Final[...]` configuration; no mutable singletons added.
- A-LAYER-03: PASS — concrete instantiations (`BasicRiskEngine`, `MemoryPositionStore`, `BacktestOrderRouter`, `PassiveLimitOrderRouter`, `BudgetBasedSizer`, `SignalPositionTranslator`) still only in `src/feelies/bootstrap.py` and `tests/`. The new `compute_artifact_id` / `_demo_data_version` / `_live_data_version` helpers in `scripts/run_backtest.py` are pure functions over data structures, no instantiation of layer components.

### A2. Event-bus & typed-schema discipline
- A-EVENT-01: PASS — `bus.publish(` count unchanged from baseline (`src/feelies/kernel/orchestrator.py:39` hits, all `Event` subclasses).
- A-EVENT-02: PASS — `EventBus.subscribe` typed-handler enforcement unchanged.
- A-EVENT-03: PASS — `Event` subclass constructors unchanged; `correlation_id`, `timestamp_ns`, `sequence` still required.

### A3. Clock abstraction
- A-CLOCK-01: PASS — wall-clock hits remain in three allowed sites (`core/clock.py`, `monitoring/structured_logging.py`, `ingestion/massive_ingestor.py:209-219` — `time.monotonic()` for HTTP page-latency telemetry, not replay-relevant). No new violations.
- A-CLOCK-02: PASS — `SimulatedClock.set_time` monotonicity guard exercised by `tests/validation/test_determinism_props.py::TestSimulatedClockProperties::test_simulated_clock_never_moves_backward`.

### A4. State-machine integrity
- A-SM-01: PASS — five SMs present and unchanged.
- A-SM-02: PASS — `StateMachine.__init__` enum-completeness check exercised by `TestEnumCompleteness` (3 tests, all pass).
- A-SM-03: PASS — `rg "except IllegalTransition" src/` returns zero hits.
- A-SM-04: PASS — orchestrator wires `TransitionRecord` callback at boot.
- A-SM-05: PASS — `unlock_from_lockdown(audit_token=...)` with zero-exposure guard unchanged.

### A5. Backtest/live parity surface
- A-PARITY-01: PASS — `OperatingMode.` / `backend.mode` hits outside `bootstrap.py`/`platform_config.py` remain in docstrings only ([orchestrator.py:202](../src/feelies/kernel/orchestrator.py)). No mode-discriminating logic.
- A-PARITY-02: PASS — `_process_tick` unchanged; no mode branch introduced.
- A-PARITY-03: PASS — `_create_backend` table unchanged; backtest is the only wired backend.

### A6. Fail-safe defaults
- A-FAIL-01: PASS — explicit `RiskAction.{ALLOW,SCALE_DOWN,REJECT,FORCE_FLATTEN}` branches plus `else: raise ValueError(...)` at both M5 ([orchestrator.py:843-872](../src/feelies/kernel/orchestrator.py)) and M6 ([orchestrator.py:906-947](../src/feelies/kernel/orchestrator.py)).
- A-FAIL-02: PASS — `_apply_ack_to_order` unchanged; raises on unknown `OrderAckStatus`.
- A-FAIL-03: PASS — `KillSwitch.activate` irreversible without `unlock_from_lockdown(audit_token=...)`.
- A-FAIL-04: PASS — `CompositeSignalEngine.evaluate` returns `None` on `warm=False` or `stale=True`; unchanged.

### A7. Determinism & provenance
- A-DET-01: PASS — `uuid4` hits in `kernel/orchestrator.py` are docstrings only (lines 23, 2038); no runtime use.
- A-DET-02: **PASS — was BLOCKER, now resolved.** `python scripts/run_backtest.py --demo` run twice → bit-identical `parity_hash` `390f2f4e…94f6`. Root-cause fix landed in two places: (a) [`src/feelies/core/platform_config.py:_to_dict`](../src/feelies/core/platform_config.py) now folds path-based fields (`alpha_spec_dir`, `alpha_specs`, `data_dir`, `event_log_path`) into the snapshot by **basename only**, blocking absolute paths from leaking into `checksum`; (b) [`scripts/run_backtest.py:run_demo`](../scripts/run_backtest.py) now uses a stable `tempfile.gettempdir()/feelies_demo_workspace` location instead of randomised `tempfile.mkdtemp()`. Both changes verified by inline experiments.
- A-DET-03: **PASS — was MAJOR, now resolved.** Direct verification: `python -c "cfg=PlatformConfig(...); print(cfg.snapshot().checksum == cfg.snapshot().checksum)"` → `True` (`73d6728b9f922f86…cf0ce`). Wrapper `timestamp_ns` still differs across calls but is metadata; `checksum` is now stable because `_to_dict()` no longer pulls in non-deterministic input.
- A-DET-04: **PASS — was MAJOR, now resolved (re-classification).** Re-reading [`scripts/run_backtest.py:_resequence`](../scripts/run_backtest.py) line 208: `events.sort(key=lambda e: e.exchange_timestamp_ns)` operates on the concatenated `all_events` list across all symbols and days. Cross-symbol global sort IS enforced. The baseline finding mis-classified this; corrected here.

### A8. Performance measurement plumbing
- A-PERF-01: PASS — `tick_to_decision_latency_ns` emitted as `MetricEvent` at M10 (visible in demo report as p50/p95/p99 lines).
- A-PERF-02: MINOR — per-segment scoped timers around M0->M10 are still implicit; not measured per micro-step. Carried.
- A-PERF-03: PASS — no eager-format `f"…"` logging without level guard inside `_process_tick`.
- A-PERF-04: PASS — `rg "asyncio|Thread\(|concurrent\.futures|multiprocessing" src/feelies/kernel/orchestrator.py` returns no matches. Single-thread invariant intact.

### A8b. Performance budget enforcement
- A-PERFB-01: PASS — `pytest -q` total wall time 47.71s vs baseline 47.35s (+0.8%, well within ±25%).
- A-PERFB-02: PASS — demo p99 = 0.088 ms (88 µs), 113× under the 10 ms hard ceiling and 34× under the 3 ms target.
- A-PERFB-03: PASS (informational, demo data trivial) — 8 ticks executed end-to-end in <0.1 s; events/sec metric not meaningful at this dataset size.

### A9. Test-coverage spine
- A-TEST-01: PASS — every `src/feelies/<layer>/` has a sibling `tests/<layer>/` directory (unchanged).
- A-TEST-02: **PASS — was MAJOR, now resolved.** All 9 testing-validation invariants now have at least one Hypothesis-backed property test in [`tests/validation/test_determinism_props.py`](../tests/validation/test_determinism_props.py): clock monotonicity (`TestSimulatedClockProperties`), idempotent submission (`TestOrderIdDeterminism`), SM validity (`TestStateMachineProperties`), enum completeness (`TestEnumCompleteness`), monotonic risk (`TestRiskEscalation`), position conservation (`TestPositionConservation`), micro pipeline returns to M0 (`TestMicroPipelineReturn`), deterministic replay (`TestCheckpointRestore`), **PnL decomposition (`TestPnLDecomposition` — added this iteration)**. Causal ordering is covered transitively by `TestPositionConservation` + `TestSimulatedClockProperties`.
- A-TEST-03: PASS — `pytest -q` exit 0; `1042 passed, 3 skipped` (delta vs baseline: **+2 passed**, 0 new failures).

### A10. Data ingestion fidelity
- A-DATA-01: PASS — `NBBOQuote(` / `Trade(` constructions outside ingestion live only in `src/feelies/core/events.py` (the dataclass definitions themselves) and `tests/`. No regression.
- A-DATA-02: PASS — `MassiveNormalizer._check_gap` flips `DataHealth → GAP_DETECTED`; recovery path back to `HEALTHY` exists.
- A-DATA-03: PASS — `duplicates_filtered` counter + `IngestResult` reporting unchanged.
- A-DATA-04: PASS — `make_correlation_id` assigned at ingestion boundary; no downstream reassignment.
- A-DATA-05: PASS — `rg "def (update|delete|remove|pop)" src/feelies/storage/` returns no matches; `EventLog` implementations expose only `append` / `append_batch`.
- A-DATA-06: **MAJOR (permanent until fixed).** [`src/feelies/core/serialization.py`](../src/feelies/core/serialization.py) defines the `EventSerializer` protocol but no concrete implementation is exposed; [`src/feelies/storage/disk_event_cache.py`](../src/feelies/storage/disk_event_cache.py) exists but is not wired into `bootstrap.py`. Status unchanged from baseline; carried in risk register.

### A11. Regime engine integration
- A-REGIME-01: PASS — `bootstrap.build_platform` composes a single `RegimeEngine` via `get_regime_engine(name)`.
- A-REGIME-02: PASS — `RegimeState` mutation confined to `services/regime_engine/`.
- A-REGIME-03: PASS — `BudgetBasedSizer` applies regime scalars.
- A-REGIME-04: **MAJOR (permanent until forensics regime online).** Conservative-divergence rule remains documented-only; no forensic regime classifier integrated. Carried.
- A-REGIME-05: **MAJOR.** Alpha YAML under `alphas/trade_cluster_drift/` does not yet reference a versioned `regime_calibration.json` artifact. Carried.

### A12. Safety mechanisms triad
- A-SAFE-01: PASS — `KillSwitch` present; irreversible without audit-token unlock; zero-exposure guard enforced.
- A-SAFE-02: **BLOCKER for promotion (permanent).** No circuit-breaker class distinct from `KillSwitch`. Carried.
- A-SAFE-03: **BLOCKER for promotion (permanent).** No capital-throttle class distinct from `KillSwitch`. Carried.
- A-SAFE-04: PASS — `KillSwitchActivation` event emitted; circuit-breaker / throttle equivalents will need their own typed events when added.
- A-SAFE-05: PASS for the only mechanism present (`KillSwitch`); revisits when other mechanisms land.

### A13. Persistence durability
- A-PERSIST-01: MAJOR (informational) — in-memory backend inventory unchanged: `InMemoryEventLog`, `InMemoryTradeJournal`, `InMemoryFeatureSnapshotStore`, `InMemoryKillSwitch`, `InMemoryAlertManager`, `InMemoryMetricCollector` (see `bootstrap.py:41-144`).
- A-PERSIST-02: **BLOCKER for promotion (permanent).** No durable backends wired. Carried.
- A-PERSIST-03: PASS — `build_platform(config)` called twice on the same config produces identical `config_snapshot.checksum` (verified by `tests/test_bootstrap.py::TestBootstrap::test_config_snapshot_checksum_is_deterministic`).
- A-PERSIST-04: **MAJOR.** No explicit feature-snapshot warm-start round-trip test (checkpoint → restore → next event = same `FeatureVector`). `tests/validation/test_determinism_props.py::TestCheckpointRestore` exercises checkpoint write but not the round-trip equality. Carried.

### A14. Secrets & credentials handling
- A-SEC-01: PASS — `MASSIVE_API_KEY` referenced only via `os.getenv("MASSIVE_API_KEY")` in `scripts/run_backtest.py:1051`; no literal credentials in `src/`, `alphas/`, or `platform.yaml`. No `BROKER_*`, `password=`, or `Bearer ` literals anywhere.

## Pillar B — Causal Chain

### B1. Mechanism-before-trade
- B-MECH-01: PASS — both `alphas/_template/template.alpha.yaml` and `alphas/trade_cluster_drift/trade_cluster_drift.alpha.yaml` declare `hypothesis:` and `falsification_criteria:`.
- B-MECH-02: **MAJOR (carried).** `falsification_criteria` for `trade_cluster_drift` references metrics qualitatively; not yet bound to a forensics-computable predicate. Carried.
- B-MECH-03: PASS — `risk_budget:` block present; `AlphaBudgetRiskWrapper` consumes it.
- B-MECH-04: PASS — hypothesis text names a structural force ("trade clustering drift").

### B2. Causality / no-lookahead
- B-CAUSAL-01: PASS — `trade_cluster_drift.computation` `update(quote)` consumes only `quote` plus internal state.
- B-CAUSAL-02: PASS — `CompositeSignalEngine.evaluate` short-circuits to `None` on `warm=False` or `stale=True`.
- B-CAUSAL-03: PASS — `BacktestOrderRouter` and `PassiveLimitOrderRouter` price decisions against the latest NBBO at ack time.
- B-CAUSAL-04: PASS — `Clock.now_ns()` hits in `kernel/orchestrator.py` (16 sites) and `ingestion/{massive_ingestor,massive_ws,replay_feed}.py` (3 sites) are all administrative timestamps on `MetricEvent`/`KillSwitchActivation`/`RegimeState`/etc., or HTTP page-arrival monitoring (`received_ns`) — never the replay-relevant `exchange_timestamp_ns` of an `NBBOQuote`/`Trade`. No regression.

### B3. Two-phase risk gate
- B-RISK-01: PASS — `_process_tick` calls `risk.check_signal` (M5) and `risk.check_order` (M6); no `OrderRequest` constructed outside this path.
- B-RISK-02: PASS — `RiskAction.NO_ACTION` (i.e. signal-level rejection) skips M5–M9 and falls through to M10 metrics.
- B-RISK-03: PASS — `SCALE_DOWN` applies `verdict.scaling_factor`, then re-runs `check_order`.
- B-RISK-04: **MAJOR (carried).** Drawdown gate thresholds in `risk/basic_risk.py` and the `risk-engine` skill table aren't explicitly cross-checked in code; documentation of any divergence still pending.

### B4. Position sizing & intent translation
- B-SIZE-01: PASS — `BudgetBasedSizer.compute_target_quantity` consumes regime scalar.
- B-SIZE-02: PASS — caps at alpha-declared `max_position_per_symbol`.
- B-SIZE-03: PASS — `SignalPositionTranslator.translate` covers full `(SignalDirection × Position)` matrix.
- B-SIZE-04: PASS — `platform_min_order_shares` enforced once, post-sizing pre-routing.

### B5. Order-lifecycle & idempotency
- B-ORDER-01: PASS — `_active_orders` keyed by SHA-256 ids; entries removed only on terminal state.
- B-ORDER-02: PASS — ack switch exhaustive over `OrderAckStatus`; auto-acks `SUBMITTED` on early `FILLED`.
- B-ORDER-03: PASS — `cancel_order` only `ACKNOWLEDGED → CANCEL_REQUESTED`; rejects `PARTIALLY_FILLED`.
- B-ORDER-04: MAJOR (carried, as baseline) — duplicate `order_id` from replay is implicitly dropped via `_active_orders` membership but not asserted at the router boundary; no regression.

### B6. Fill model realism
- B-FILL-01: PASS — `DefaultCostModelConfig` carries spread, commission (taker/maker), adverse selection, regulatory, stress multiplier.
- B-FILL-02: PASS (informational) — `--demo --stress-cost 1.5` ran end-to-end; net PnL = $0.00 (no trades on the 8-tick demo). Stress-cost path exercised; no negative outcome to quarantine. **Permanent caveat (action queue #7):** demo data must be augmented to actually fill before this check has teeth.
- B-FILL-03: PASS — `PassiveLimitOrderRouter` has through-fill, level-fill, and `passive_max_resting_ticks` cancel.
- B-FILL-04: PASS — stop-loss / emergency exits route via MARKET (Inv 11 honoured).

### B7. Position & PnL reconciliation
- B-PNL-01: PASS — `_reconcile_fills` updates `MemoryPositionStore` atomically and emits `PositionUpdate` + `TradeRecord`.
- B-PNL-02: PASS — `TradeRecord` carries `requested_quantity`, `filled_quantity`, `fill_price`, `cost_bps`, `fees`, `realized_pnl`, all required ts.
- B-PNL-03: **MAJOR (carried).** No multi-alpha demo path exists yet to exercise `StrategyPositionStore` + `FillAttributionLedger` summation against portfolio totals.
- B-PNL-04: PASS — per-tick MtM DD computed against last NBBO mid; `_escalate_risk` path wired.

### B8. End-to-end live-practice replay
- B-E2E-01: PASS — `--demo` run completed; trade count=0, gross/net PnL=$0.00, max DD=$0.00 (0.00%), kill switch INACTIVE, parity hash captured (see header).
- B-E2E-02: **PASS — was BLOCKER (downstream of A-DET-02), now resolved.** Two consecutive `--demo` invocations produce bit-identical `pnl_hash`, `config_hash`, `parity_hash`, **and** `artifact_id`. See header.
- B-E2E-03: PASS (informational) — both `--stress-cost 1.5` and `--stress-cost 2.0` completed; net PnL = $0 (no trades). Stress path executes; no negative outcome at portfolio level.
- B-E2E-04: **MAJOR (carried, multiple gaps).** `tests/test_backtest_e2e.py` exists; explicit scenario stubs still missing for warm-up, regime transition, circuit-breaker firing, gap recovery, multi-alpha arbitration. Carried.
- B-E2E-05: **BLOCKER for promotion (permanent).** Sim-vs-live divergence harness absent until a paper router exists. Carried.

### B9. Provenance & audit trail
- B-PROV-01: PASS — `TradeRecord` carries `correlation_id`, `signal_timestamp_ns`, `submit_timestamp_ns`, `fill_timestamp_ns`.
- B-PROV-02: PASS — `EventLog.append` invoked at M1 (per quote) and per fill in `_reconcile_fills`.
- B-PROV-03: PASS — `PlatformConfig.snapshot().checksum` = `cf9b8c31…5f34` recorded in this report's header; now diff-stable across iterations (A-DET-03 fix).
- B-PROV-04: PASS — both alpha YAMLs declare `version` + `schema_version`; `AlphaRegistry.lifecycle` recorded as `RESEARCH`.

### B10. Personal-trading guardrails
- B-GUARD-01: PASS — stop-loss evaluated per tick from last NBBO mid; exit issued via MARKET; not gated by passive cooldowns.
- B-GUARD-02: PASS — trailing-stop peak monotonic per-position; resets on flat/flip.
- B-GUARD-03: PASS — `signal_entry_cooldown_ticks` per-(symbol, strategy); resets on EXIT.
- B-GUARD-04: PASS — `platform_min_order_shares` enforced at exactly one site, post-sizing.
- B-GUARD-05: **MAJOR (carried).** `account_equity` semantics (start-of-day NAV vs live MtM) still undocumented.
- B-GUARD-06: **MAJOR (carried).** `float` annotations on percent fields in `risk/basic_risk.py` not yet migrated to `Decimal`. Money fields in `Position`, `TradeRecord`, `OrderRequest` remain `Decimal`.

### B11. Multi-alpha arbitration & attribution
- B-MULTI-01: PASS — `MultiAlphaEvaluator` wired only when `len(registry) > 1`.
- B-MULTI-02: **MAJOR (carried).** `SignalArbitrator` policy is deterministic but the tie-break/precedence rule is not exhaustively documented or property-tested.
- B-MULTI-03: PASS — `AlphaBudgetRiskWrapper` isolates per-strategy risk.
- B-MULTI-04: **MAJOR (carried).** No multi-alpha demo path yet to verify `FillAttributionLedger` sum vs `MemoryPositionStore` totals.
- B-MULTI-05: **MAJOR (carried).** `correlation_budget:` field absent from `alphas/_template/template.alpha.yaml`.

### B12. Promotion-gate readiness
- B-PROMO-01: NO — Research → Paper still gated by **permanent** A-SAFE-02, A-SAFE-03, A-PERSIST-02, B-E2E-05, A-DATA-06; **temporary** B-DECAY-02/03/04, B-MECH-02. Iteration-level blockers (A-DET-02, A-TEST-02, B-PROMO-04) are now resolved, so the iteration verdict is PASS even though the promotion verdict is still NO.
- B-PROMO-02: NO — paper router stub raises `NotImplementedError` (permanent until paper backend lands).
- B-PROMO-03: NO — depends on ≥10 trading days of small-capital live data; not applicable until B-PROMO-02 resolves.
- B-PROMO-04: **PASS — was MAJOR, now resolved.** [`scripts/run_backtest.py:compute_artifact_id`](../scripts/run_backtest.py) computes `SHA-256({strategy_version (alpha_id@version, sorted), config_version, data_version, engine_version})`; emitted in the demo report. Two consecutive `--demo` runs yield identical id `e247a17b…49f9`. Helpers `_demo_data_version()` / `_live_data_version(symbols, date_range)` provide the data-version axis.

### B13. Decay-detection plumbing
- B-DECAY-01: **MAJOR (carried, partial PASS).** `forensics/decay_detector.py::DecayDetector.analyze_fills` computes mean / p95 cost-bps, edge-bps, rolling 50/200, edge-vs-cost coverage. Per-skill names (slippage residual, hit-rate residual, fill-rate drift) still not separately implemented.
- B-DECAY-02: **MAJOR (carried).** No crowding scorecard.
- B-DECAY-03: **MAJOR (carried).** No latency-disadvantage signal computed from `signal_ts`/`submit_ts`/`fill_ts`.
- B-DECAY-04: **MAJOR (carried).** `DecaySignal → Alert → risk scaling` quarantine pathway is partially implemented but not end-to-end.

## Promotion-gate readiness summary

- Research → Paper: **NO** (blocking: permanent A-SAFE-02, A-SAFE-03, A-PERSIST-02, B-E2E-05, A-DATA-06; temporary B-DECAY-02/03/04, B-MECH-02). Iteration-level blockers are clear; promotion is gated on the permanent risk-register entries below.
- Paper → Small Capital: **NO** (blocking: paper router stub, sim-vs-live divergence harness absent).
- Small → Scaled: **NO** (downstream of Paper → Small Capital).

## Deltas vs prior audit

Prior report: `audits/2026-04-17-baseline.md`.

- New BLOCKER: **none**.
- Resolved BLOCKER: **A-DET-02** (parity hash drift via `--demo` tempdir leakage). The corresponding entry has been removed from the risk register below; the underlying mechanism (path normalisation in snapshot serialisation) is now invariant-preserving.
- Resolved MAJOR: **A-DET-03** (snapshot checksum round-trip stability), **A-DET-04** (multi-symbol global timestamp sort — re-classified after re-reading [`scripts/run_backtest.py:_resequence`](../scripts/run_backtest.py); baseline mis-classified), **A-TEST-02** (PnL-decomposition property test added), **B-E2E-02** (downstream of A-DET-02), **B-PROMO-04** (artifact id added and stable).
- New MAJOR: **none**.
- Tests delta: **+2 passed** (the two new `TestPnLDecomposition` cases). Failed/error counts unchanged at 0.

## Risk register (permanent / known findings — re-listed every iteration)

- **Persistence**: storage backends are in-memory only (`InMemoryEventLog`, `InMemoryTradeJournal`, `InMemoryFeatureSnapshotStore`, `InMemoryKillSwitch`, `InMemoryAlertManager`, `InMemoryMetricCollector`). `DiskEventCache` exists ([storage/disk_event_cache.py](../src/feelies/storage/disk_event_cache.py)) but is not wired into `build_platform`. **BLOCKER for promotion** to PAPER/LIVE.
- **No paper / live router**: `_create_backend` raises `NotImplementedError` for non-BACKTEST modes; `PaperOrderRouter` and `LiveOrderRouter` are stub classes whose `__init__` raises. **BLOCKER for promotion** to PAPER/LIVE.
- **No durable `EventSerializer` implementation**: protocol exists in [`src/feelies/core/serialization.py`](../src/feelies/core/serialization.py) but no concrete implementation is exposed; without one, `DiskEventCache` cannot enforce bit-deterministic round-trip. **MAJOR** until persisted replay is testable.
- **Sim-vs-live divergence harness absent**: cannot run drift tests until live router exists. **BLOCKER for promotion** to LIVE.
- **Circuit breaker absent**: no class distinct from `KillSwitch` that cancels open orders without flattening exists. **BLOCKER for promotion** to PAPER/LIVE.
- **Capital throttle absent**: no class distinct from `KillSwitch` that scales sizing across health signals exists. **BLOCKER for promotion** to PAPER/LIVE.
- **Session calendar absent**: no NYSE/NASDAQ market-hours awareness in the code path. **MAJOR** for paper-trading realism.
- **Conservative-divergence rule (regime)**: when forensic vs risk-state regime labels disagree, more conservative wins — not enforced because forensic regime classifier is not integrated. **MAJOR** until forensics regime is online.
- **Cross-strategy correlation budget absent**: no alpha YAML declares it; `alphas/_template/template.alpha.yaml` lacks the field. **MAJOR**.

Removed this iteration:
- ~~**Demo tempdir leakage in config snapshot**~~ — fixed by basename normalisation in [`platform_config.py:_to_dict`](../src/feelies/core/platform_config.py) plus stable workspace path in [`run_backtest.py:run_demo`](../scripts/run_backtest.py). Verified bit-identical across two consecutive runs.
- ~~**Multi-symbol global timestamp sort**~~ — re-verified: [`_resequence` line 208](../scripts/run_backtest.py) does perform a global sort by `exchange_timestamp_ns` across all symbols and days. Baseline mis-classified.

## Meta (audit-of-the-audit)

- META-01: PASS — no changes to `.cursor/rules/platform-invariants.mdc` or files under `.cursor/skills/**` since the baseline (`2026-04-17`); `git log --oneline -- .cursor/rules .cursor/skills` head matches baseline. No new check IDs required this iteration. The protocol itself remains at `v1.0`; the action queue items below are remediation, not protocol revisions, and so do not trigger a version bump.

## Action queue (carried to next iteration)

Resolved this iteration (struck through):
1. ~~Fix A-DET-02~~ — done.
2. ~~Add the missing PnL-decomposition property test (A-TEST-02)~~ — done.
4. ~~Add `B-PROMO-04` artifact-id computation~~ — done.

Carried over:
3. **Add explicit forensics implementations (B-DECAY-02/03/04)**: crowding scorecard, latency-disadvantage signal, quarantine pathway from `DecaySignal → Alert → risk scaling`.
5. **Wire `EventSerializer` concrete implementation** to enable `DiskEventCache` and clear A-DATA-06 / A-PERSIST-04.
6. **Decide and document `account_equity` semantics** (start-of-day NAV vs live MtM) for B-GUARD-05.
7. **Augment the `--demo` data set** so that 1–2 actual fills occur, giving B-FILL-02 and B-E2E-03 real teeth.
8. **Add e2e test stubs** listed under B-E2E-04 (warm-up, regime transition, circuit-breaker, gap recovery, multi-alpha arbitration).
9. **Add a `correlation_budget:` block** to `alphas/_template/template.alpha.yaml` (B-MULTI-05).
10. **Investigate `risk/basic_risk.py` float annotations** (B-GUARD-06) — confirm percent fields can stay `float` or migrate to `Decimal`.

New for this iteration:
11. **Document `SignalArbitrator` tie-break / precedence rule** (B-MULTI-02) and add a property test asserting determinism under permuted-input ordering.
12. **Cross-check drawdown gate thresholds** in [`risk/basic_risk.py`](../src/feelies/risk/basic_risk.py) against the `risk-engine` skill table; document any intentional divergence (B-RISK-04).

---

`verdict=PASS BLOCKER=0(new) MAJOR=0(new) MINOR=0(new) parity_hash_stable=Y tests_delta=+2 promotion_ready=research`
