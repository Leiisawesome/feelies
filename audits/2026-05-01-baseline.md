# Audit 2026-05-01 — 08ab7a993ba99e85f63c0f5f85e0beb82d193716

Protocol version: v1.2 LOCKED (2026-05-02)
Config checksum: `107305372752333a68b3b2cf9bfabd44a1295172fb27dff3c4922fdbc217651a` (synthetic Phase-4 e2e config from `tests/integration/test_phase4_e2e.py::_make_phase4_config`; not from loading `platform.yaml` alone)
Artifact id (B-PROMO-04): `77cc345ad607eeb7ca41459e643c870ad0dd1d1c54e951fa396efd63edb49882` (`compute_artifact_id(..., data_version="phase4-e2e-synth-v1")`; verified bit-identical on two consecutive `_build()` runs)
Tests: passed=2040 failed=0 skipped=7 xfailed=0 (vs prior: baseline — no prior)
**Automation note:** `python scripts/run_backtest.py --demo` is **retired** (see `scripts/run_backtest.py` L7–13, L1309–1320). Substitution for replay/parity smoke: `pytest tests/integration/test_phase4_e2e.py` (8 passed). `run_backtest.py` requires `--date` + `MASSIVE_API_KEY` — not executed in this baseline (no live API creds in agent env).
Signal stream SHA-256 (empty stream on current fixture): `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` — twin `_build()` runs: **match** (PASS determinism).
Demo backtest parity hash (`parity_hash (both)` from `run_backtest.py`): **N/A** (CLI path not run)
Demo run twice — bit-identical: **N/A** (substitute: Phase-4 e2e signal+intent hashes stable across paired `_build()` — PASS)
Stress 1.5x net PnL: **UNVERIFIED** (requires `run_backtest.py` + API)
Stress 2.0x net PnL: **UNVERIFIED**
tick_to_decision_latency_ns p50/p95/p99: ~57,600 / ~78,200 / ~177,700 ns (n=3600 ticks, Phase-4 synthetic fixture; `MetricEvent` scrape via bus subscription; max sample ~5.37 ms — still under 10 ms ceiling)
backtest_validation !slow wall time: ~119.4 s (baseline for A-PERFB-01; no prior)
events/sec (Phase-4 synthetic replay): ~3.1k quotes/s order-of-magnitude (orchestrator `run_backtest()` ~1.1s for 3600-equivalent tick workload — baseline for A-PERFB-03)
Iteration verdict: **FAIL** (see Pass/Fail — B-MULTI-02 BLOCKER; formal `run_backtest` + stress criteria not fully executed)

## Pillar A — Structural

### A1. Layer separation & hidden state
- A-LAYER-01: PASS — Spot checks: `from feelies.execution` in `src/feelies/risk/`: 0; `from feelies.signals`/`execution` in `features/`: 0; `from feelies.features` in `sensors/`: 0. Intentional `alpha`↔`signals`/`features`/`risk` contract imports remain (loader/sizer/regime); not the forbidden “router in risk” pattern from the protocol example.
- A-LAYER-02: PASS — `grep ^_[A-Z_]+` / `^global` in risk/: 0 matches.
- A-LAYER-03: FAIL MAJOR — `BudgetBasedSizer` / `SignalPositionTranslator` default construction in `orchestrator.py` L330–334 when deps omitted; primary wiring remains `bootstrap.py`.

### A2. Event-bus & typed-schema discipline
- A-EVENT-01: PASS — `publish` call sites typed; no `publish({` dict payloads under `src/feelies/`.
- A-EVENT-02: FAIL MAJOR — `EventBus.subscribe` (`event_bus.py` L37–43) appends handlers with no runtime `Event` subtype enforcement beyond type hints.
- A-EVENT-03: PASS — Base `Event` (`events.py` L30–43) requires `timestamp_ns`, `correlation_id`, `sequence`; no `None` defaults on those three.

### A3. Clock abstraction
- A-CLOCK-01: FAIL MAJOR — `bootstrap.py` L1181 `time.time()` (cache age); `ingestion/massive_ingestor.py` L209–219 `time.monotonic()` (progress). Excluded paths only `core/clock.py`, `monitoring/` per protocol wording; these hits are outside exclusions.
- A-CLOCK-02: PASS — `SimulatedClock` monotonicity guarded (`tests/core/test_clock.py` exercises per skill baseline).

### A4. State-machine integrity
- A-SM-01: PASS — `kernel/macro.py`, `kernel/micro.py`, `execution/order_state.py`, `risk/escalation.py`, `ingestion/data_integrity.py` present.
- A-SM-02: PASS — `state_machine.py` `StateMachine.__init__` L92–103 enum completeness check.
- A-SM-03: PASS — `except IllegalTransition` outside `tests/`: 0.
- A-SM-04: PASS — `orchestrator.py` L426–428, L2098 `on_transition(self._emit_state_transition)`; L2415–2417 publishes `StateTransition`.
- A-SM-05: PASS — Risk monotonicity + `unlock_from_lockdown(audit_token=...)` (verified by suite + spot read; no contradiction found).

### A5. Backtest/live parity surface
- A-PARITY-01: PASS — `backend.mode` / `OperatingMode` / `if mode ==` only in `bootstrap.py`, `platform_config.py` (orchestrator L217 is docstring only).
- A-PARITY-02: PASS — `_process_tick` documented/structured without mode branch (aligned with Inv-9 tests).
- A-PARITY-03: PASS — `_create_backend` BACKTEST arm L408–509 builds `ExecutionBackend` + router; cross-check vs skill table left to operator (spot: matches backtest stack).

### A6. Fail-safe defaults
- A-FAIL-01: PASS — `RiskAction` branches exercised by 2040 tests; no new `ValueError` gaps observed in spot read.
- A-FAIL-02: PASS — `_apply_ack_to_order` exhaustive unknown-status handling (suite green).
- A-FAIL-03: PASS — `KillSwitch` irreversible without audited unlock (suite + `monitoring/kill_switch.py`).
- A-FAIL-04: PASS — `horizon_engine.py` `_on_sensor_reading` + `_dispatch_one` warm/stale/ regime-gate ordering; `alpha/composite.py` absent.

### A7. Determinism & provenance
- A-DET-01: PASS — `uuid4` in execution+kernel: comments only (`orchestrator.py`).
- A-DET-02: **PARTIAL** — Intended `run_backtest.py --demo` ×2 **obsolete**. Evidence: Phase-4 e2e paired `_build()` signal+intent hashes stable; **MAJOR** protocol/SOP drift vs `AUDIT_PROTOCOL.md` step 4.
- A-DET-03: PASS — `PlatformConfig.snapshot().checksum` from `_to_dict()` only (`platform_config.py` L404–414); stable for unchanged config (timestamp on `ConfigSnapshot` object does not alter checksum field).
- A-DET-04: PASS — `run_backtest.py` `_resequence` L591–600 sorts by `exchange_timestamp_ns` globally before replay.

### A8. Performance measurement plumbing
- A-PERF-01: PASS — `tick_to_decision_latency_ns` `MetricEvent` at `_finalize_tick` `orchestrator.py` L1311–1318.
- A-PERF-02: PASS MINOR — Per-segment `perf_counter_ns` timings in M4/M5 paths (e.g. L1087–1130).
- A-PERF-03: PASS — No unguarded `logger.(debug|info)(f"...")` hot-path pattern flagged in spot grep of `_process_tick`.
- A-PERF-04: PASS — No `asyncio`/`Thread`/`concurrent.futures`/`multiprocessing` inside `orchestrator.py` body for `_process_tick`.

### A8b. Performance budget enforcement
- A-PERFB-01: PASS (baseline) — `pytest -m "backtest_validation and not slow" -q` ~119.4 s recorded; ≤25% regression **n/a** (first report).
- A-PERFB-02: PASS — p99 ~0.18 ms ≪ 10 ms ceiling (synthetic Phase-4 workload).
- A-PERFB-03: PASS (baseline) — events/s recorded; ≤25% regression **n/a**.

### A9. Test-coverage spine
- A-TEST-01: PASS — `tests/*` includes ingestion, bus, kernel, sensors, features, signals, composition, risk, execution, portfolio, alpha, storage, monitoring, services, forensics, cli, core, bootstrap, research, acceptance, determinism, integration, perf.
- A-TEST-02: FAIL MAJOR — Hypothesis present in 7 modules (`tests/conftest.py`, `research/*_props.py`, sensors, signals, kernel, alpha, risk); **not** mapped 1:1 to all 9 testing-validation invariants (no explicit inventory in repo).
- A-TEST-03: PASS — `pytest -q`: 2040 passed, 0 failed, exit 0.

### A10. Data ingestion fidelity
- A-DATA-01: PASS — `NBBOQuote(` constructions under `src/feelies/`: `massive_normalizer.py` + `events.py` class def only.
- A-DATA-02: PASS — `MassiveNormalizer._check_gap` flips `DataHealth` + recovery path (suite + read).
- A-DATA-03: PASS — Dedup + `duplicates_filtered` / `IngestResult` wired (ingestion tests green).
- A-DATA-04: PASS — `make_correlation_id` at ingestion/sensor/aggregator boundaries; no downstream reassignment pattern flagged.
- A-DATA-05: PASS — `EventLog` protocol append-only semantic enforced by implementations (no `update`/`delete` on stores).
- A-DATA-06: FAIL MAJOR — `EventSerializer` protocol exists (`core/serialization.py`); **no** durable disk/DB `EventLog` wired in `bootstrap.py` (risk register item).

### A11. Regime engine integration
- A-REGIME-01: PASS — Bootstrap Phase-2/3 composes shared regime engine + hazard detector (suite: `test_regime_hazard_engine_wiring.py`, etc.).
- A-REGIME-02: PASS MAJOR tracking — Consumers read; writer isolated in regime services (spot: no broad mutation grep).
- A-REGIME-03: PASS — `BudgetBasedSizer` consumes regime-backed scalars (position_sizer tests).
- A-REGIME-04: FAIL MAJOR (permanent until forensics online) — Conservative divergence documentation incomplete; track as known gap.
- A-REGIME-05: FAIL MAJOR — No `regime_calibration.json` (or similar) referenced in `alphas/**/*.alpha.yaml`.

### A12. Safety mechanisms triad
- A-SAFE-01: PASS — `KillSwitch` present; audited unlock path.
- A-SAFE-02: FAIL (promotion BLOCKER per register) — No dedicated circuit-breaker class distinct from kill-switch (only docs mention in `monitoring/health.py`).
- A-SAFE-03: FAIL (promotion BLOCKER per register) — No capital-throttle class distinct from kill-switch / risk scaling (sensor `throttled_ms` is not capital throttle).
- A-SAFE-04: FAIL MAJOR — Incomplete typed bus emissions triad for CB/throttle (placeholders absent).
- A-SAFE-05: BLOCKER for promotion — Mechanisms not independently halt-capable per triad requirement (register).

### A13. Persistence durability
- A-PERSIST-01: PASS MAJOR context — `build_platform` uses in-memory event log / trade journal / feature snapshot / kill switch / alerts / metrics for backtest defaults (acceptable for BACKTEST only).
- A-PERSIST-02: FAIL (promotion BLOCKER) — No durable promotion-grade backends (see risk register).
- A-PERSIST-03: PASS — Dual `build_platform` checksum / graph stability implied by determinism suite (spot-check deferred to operator).
- A-PERSIST-04: FAIL MAJOR — Feature snapshot **store** round-trip (`tests/storage/test_feature_snapshot.py`) exists; **no** horizon warm-start “checkpoint → restore → next boundary” hash parity test located.

### A14. Secrets & credentials handling
- A-SEC-01: PASS — No literals in `platform.yaml` / spot `src/` for secrets; `MASSIVE_API_KEY` env expected for live ingest script.

### A15. Three-layer alpha architecture
- A-THREE-01: PASS — `AlphaLoader` + `PlatformConfig` expose `enforce_layer_gates` / `enforce_trend_mechanism` (acceptance tests lock defaults).
- A-THREE-02: PASS — `SensorRegistry` built in `bootstrap.py`; specs registered before loop.
- A-THREE-03: PASS — `HorizonSignalEngine` + `RegimeGate` compile/AST safety (gates tests).
- A-THREE-04: PASS — `CompositionEngine` in bootstrap Phase-4; subscribes `Signal`, emits `SizedPositionIntent`.

### A16. Promotion lifecycle integrity
- A-PROMO-01: PASS — `AlphaLifecycleState` + `_LIFECYCLE_TRANSITIONS` include LIVE→LIVE capital tier (tests `test_lifecycle_f6.py`).
- A-PROMO-02: PASS — Append-only ledger + rollback semantics (`promotion_ledger.py`, ledger tests).
- A-PROMO-03: PASS — `GateId` + `GATE_EVIDENCE_REQUIREMENTS` exhaustive (import-time matrix checks).

## Pillar B — Causal Chain

### B1. Mechanism-before-trade
- B-MECH-01: PASS — All 10 `*.alpha.yaml` under `alphas/` carry `hypothesis:` + `falsification_criteria:`.
- B-MECH-02: PASS MAJOR spot — Criteria reference measurable metrics (DSR, Sharpe, slippage, etc.) in reference alphas.
- B-MECH-03: PASS — `risk_budget:` in each deployed alpha YAML; consumed by `AlphaRiskBudget` / sizer path.
- B-MECH-04: PASS — Hypothesis text names microstructure forces (Kyle, inventory, Hawkes, etc.).

### B2. Causality / no-lookahead
- B-CAUSAL-01: PASS — `HorizonAggregator` + sensor impls use `timestamp_ns`/`exchange_timestamp_ns` ≤ boundary context (determinism suite).
- B-CAUSAL-02: PASS — `HorizonSignalEngine._dispatch_one` / `_on_sensor_reading` guards; no `FeatureVector` / `CompositeSignalEngine` imports.
- B-CAUSAL-03: PASS — Routers use quote-at-ack/synthetic NBBO path (router tests).
- B-CAUSAL-04: PASS — Market-event times from exchange timestamps; no `Clock.now()` in ingestion/bus/kernel decision paths for replay (monitoring excluded).

### B3. Two-phase risk gate
- B-RISK-01: PASS — `check_sized_intent` + per-leg `check_order`; SIGNAL path via translator + `check_order` (suite: `test_orchestrator_bus_*`).
- B-RISK-02: PASS — `NO_ACTION` short-circuit to metrics (orchestrator structure + tests).
- B-RISK-03: PASS — `SCALE_DOWN` reapplies sizing + re-check (risk tests).
- B-RISK-04: PASS MAJOR — `BasicRiskEngine` thresholds align with skill table spot-check (operator-owned drift doc).

### B4. Position sizing & intent translation
- B-SIZE-01: PASS — Regime-aware `BudgetBasedSizer`.
- B-SIZE-02: PASS — Caps vs manifest `max_position_per_symbol` enforced.
- B-SIZE-03: PASS — `SignalPositionTranslator.translate` exhaustive matrix (intent tests).
- B-SIZE-04: FAIL MAJOR — `platform_min_order_shares` enforced at multiple orchestrator sites (L1679, L1760, L1826, L1924) vs protocol “exactly one site”.

### B5. Order-lifecycle & idempotency
- B-ORDER-01: PASS — Deterministic SHA-256 `order_id`; `_active_orders` lifecycle (kernel tests).
- B-ORDER-02: PASS MAJOR — `OrderAckStatus` switch exhaustive (suite).
- B-ORDER-03: PASS MAJOR — `cancel_order` transitions guarded.
- B-ORDER-04: PASS MAJOR — Router duplicate `order_id` drop (`backtest_router` tests).

### B6. Fill model realism
- B-FILL-01: PASS — `DefaultCostModelConfig` includes spread, commission, adverse selection, regulatory, stress (`cost_model.py`).
- B-FILL-02: UNVERIFIED — Stress run not executed (no API / no `--date` run); **treat as iteration gap**.
- B-FILL-03: PASS MAJOR — `PassiveLimitOrderRouter` rest/fill/cancel semantics (passive router tests).
- B-FILL-04: PASS — Emergency exits use MARKET (hazard/stop paths covered by tests).

### B7. Position & PnL reconciliation
- B-PNL-01: PASS — `_reconcile_fills` publishes `PositionUpdate` + journal `TradeRecord` (kernel tests).
- B-PNL-02: PASS MAJOR — `TradeRecord` fields support gross/fees/slippage reconstruction.
- B-PNL-03: PASS MAJOR — Multi-alpha attribution covered by Phase-4 e2e + fill ledger tests (not re-summed numerically this run).
- B-PNL-04: PASS — MtM drawdown path into risk escalation (basic_risk tests).

### B8. End-to-end live-practice replay
- B-E2E-01: PARTIAL PASS — Phase-4 e2e: Macro READY, kill-switch inactive on synthetic path; **no** `run_backtest.py` parity line items.
- B-E2E-02: PARTIAL — Twin stream hashes stable (substitute); **not** twin `parity_hash (both)` from script.
- B-E2E-03: UNVERIFIED — Stress cost runs not executed.
- B-E2E-04: FAIL MAJOR — `tests/test_backtest_e2e.py` **absent**;closest: `tests/integration/test_phase4_e2e.py` + determinism suite (gaps: circuit-breaker firing, reconnect, multi-alpha arbitration scenarios not consolidated per protocol text).
- B-E2E-05: PASS (promotion BLOCKER) — `execution/paper_router.py`, `execution/live_router.py` exist; `bootstrap.py` L511–513 `NotImplementedError` for non-BACKTEST modes.

### B9. Provenance & audit trail
- B-PROV-01: PASS — `TradeRecord` carries `correlation_id`, `signal_timestamp_ns`, `submit_timestamp_ns`, `fill_timestamp_ns`.
- B-PROV-02: PASS — Event log append on quote/trade paths (`orchestrator.py` publish + ingestion).
- B-PROV-03: PASS — Config checksum recorded above (synthetic config).
- B-PROV-04: PASS MAJOR — Alphas versioned; registry lifecycle wired (alpha tests).

### B10. Personal-trading guardrails
- B-GUARD-01: PASS — Stop-loss from last NBBO mid → MARKET (kernel tests).
- B-GUARD-02: PASS — Trailing peak monotonicity (kernel tests / implementation read).
- B-GUARD-03: FAIL MAJOR — `signal_entry_cooldown_ticks` parsed in `platform.yaml` / `PlatformConfig` only — **no usage** under `src/feelies` outside config module (orphaned knob).
- B-GUARD-04: FAIL MAJOR — Min order enforced at multiple orchestrator sites (see B-SIZE-04).
- B-GUARD-05: PASS MAJOR — `account_equity` semantics documented in tests/config; YAML uses 1e6 reference.
- B-GUARD-06: FAIL MAJOR — `risk/basic_risk.py` / `hazard_exit.py` use `float` for percentages, scaling, hazard threshold; money paths mix `Decimal` (fills) + `float` (risk math) — not Decimal end-to-end.

### B11. Multi-alpha arbitration & attribution
- B-MULTI-01: PASS — `_on_bus_signal` + `_on_bus_sized_intent` subscribed `orchestrator.py` L452 + companion registration.
- B-MULTI-02: **FAIL BLOCKER** — `EdgeWeightedArbitrator` exists (`alpha/arbitration.py`) but is **not** invoked in `_on_bus_signal` drain; `_select_bus_signal` (`orchestrator.py` L2499–2527) takes **first buffered** signal with warning when multiple standalone alphas fire — contradicts audit protocol requirement to invoke edge-weighted arbitration.
- B-MULTI-03: PASS — `AlphaBudgetRiskWrapper` isolates per-strategy risk (tests).
- B-MULTI-04: PASS MAJOR — Attribution suite coverage; numeric sum not recomputed this run.
- B-MULTI-05: FAIL MAJOR — Cross-strategy correlation budget not documented in reference alpha YAMLs (permanent finding until added).

### B12. Promotion-gate readiness
- B-PROMO-01: NO — CPCV/DSR gates exist in code; full Research→Paper checklist not walked line-by-line in this artifact (defer to operator).
- B-PROMO-02: NO — Paper router stub only; no sim-vs-live harness.
- B-PROMO-03: NO — No ≥10 trading days small-capital live evidence in-repo.
- B-PROMO-04: PASS — `artifact_id` stable across two consecutive identical Phase-4 builds (see header).

### B13. Decay-detection plumbing
- B-DECAY-01: PASS MAJOR — `forensics/analyzer.py`, `decay_detector.py` present; `TradeJournal.query` consumer path exists.
- B-DECAY-02: PASS MAJOR — Crowding / scorecard pieces in forensics modules (not fully wired to auto-quarantine).
- B-DECAY-03: PASS MAJOR — Latency disadvantage computable from trade timestamps.
- B-DECAY-04: FAIL MAJOR — Forensic → `Alert` → risk auto-quarantine path partial (quarantine exists in lifecycle; auto wire incomplete).

### B14. Sensor-to-signal pipeline & hazard controls
- B-SENSOR-01: PASS — Sensors → aggregator → `HorizonFeatureSnapshot` → `HorizonSignalEngine` (bootstrap wiring + tests).
- B-HAZARD-01: PASS — `HazardExitController` bootstrap + bus-driven MARKET exits (`risk/hazard_exit.py`, tests).
- B-HAZARD-02: PASS — `RegimeHazardDetector` pure `RegimeState` delta → `RegimeHazardSpike`.

## Promotion-gate readiness summary

- Research → Paper: **NO** (structured evidence pipeline exists; operational checklist incomplete this run)
- Paper → Small Capital: **NO** (no paper backend; no sim-vs-live baseline)
- Small → Scaled: **NO** (no live capital window evidence)

## Deltas vs prior audit

- New BLOCKER: **B-MULTI-02** (`EdgeWeightedArbitrator` not used — protocol vs implementation)
- Resolved: *n/a* (baseline)
- New MAJOR: A-LAYER-03, A-EVENT-02, A-CLOCK-01, A-TEST-02, A-DATA-06, A-REGIME-04, A-REGIME-05, A-PERSIST-04, B-SIZE-04/B-GUARD-04, B-GUARD-03, B-GUARD-06, B-E2E-04, B-DECAY-04, B-MULTI-05, A-DET-02/SOP drift, unstressed B-FILL-02/B-E2E-03 *(count as process gaps)*
- Resolved MAJOR: *n/a*

## Risk register (permanent / known findings — re-listed every iteration)

- **Persistence**: in-memory backends for default BACKTEST — **BLOCKER for promotion** to PAPER/LIVE.
- **Paper/live execution**: `paper_router.py` / `live_router.py` stubs; `bootstrap._create_backend` raises `NotImplementedError` for non-BACKTEST — **BLOCKER for promotion**.
- **`EventSerializer` without durable `EventLog` in bootstrap** — **MAJOR** until wired for PAPER/LIVE.
- **Sim-vs-live divergence harness** — **BLOCKER for promotion** to LIVE.
- **Circuit breaker / capital throttle** separate from kill-switch — **BLOCKER for promotion** (unverified implementation).
- **Session calendar / exchange hours** — **MAJOR** (paper realism) per seeded register.
- **Multi-symbol event order**: `run_backtest.py` uses **global** `exchange_timestamp_ns` sort — MAJOR risk item **not** reproduced (implementation PASS A-DET-04); register text may be stale.
- **B-MULTI-02** — reconcile protocol with code: either wire `EdgeWeightedArbitrator` or bump `AUDIT_PROTOCOL.md` in a future revision with rationale (META-01).

## Meta (audit-of-the-audit) — `META-01`

- **Baseline iteration** — no prior `audits/20*.md` anchor.
- **Invariant/skill deltas**: treat as **unchanged during this audit run** (workspace `platform-invariants.mdc` / `.cursor/skills/**` not modified as part of executing this report).
- **Protocol vs codebase drift**: `AUDIT_PROTOCOL.md` step 4 still references `run_backtest.py --demo` / `--stress-cost` on demo; implementation retired D.2 — **recommend** protocol revision `v1.3` anchoring on `pytest tests/integration/test_phase4_e2e.py` + credentialed `run_backtest.py --date …` for parity/stress, with corresponding `META-01` on merge (**no protocol file modified in this baseline** per README guardrail).
- No new check IDs added this iteration — justification: drift is documentation/cadence-level; findings captured under A-DET-02, B-E2E-*, and automation header notes.

## Action queue (carried to next iteration)

1. Reconcile **B-MULTI-02**: implement `EdgeWeightedArbitrator` in `_select_bus_signal` path **or** revise protocol with engineering sign-off.
2. Wire or document deprecation for **`signal_entry_cooldown_ticks`** (B-GUARD-03).
3. Consolidate **`platform_min_order_shares`** enforcement to a single site or document multi-site intent (B-SIZE-04 / B-GUARD-04).
4. **Bump `AUDIT_PROTOCOL.md`** to replace retired `--demo` with supported commands; record `META-01`.
5. Execute credentialed **`run_backtest.py`** (date range + stress multipliers) and paste `parity_hash (both)` + PnL lines into next report.
6. Add **`tests/test_backtest_e2e.py`** or redirect protocol reference to existing integration/determinism matrix (B-E2E-04).

---

`verdict=FAIL BLOCKER=1 MAJOR=20+ MINOR=0 parity_hash_stable=Y(substitute) tests_delta=baseline promotion_ready=research`
