# Audit 2026-05-01 — iter-02 — 08ab7a993ba99e85f63c0f5f85e0beb82d193716

Protocol version: v1.3 LOCKED (2026-05-03)
Prior report anchor: `audits/2026-05-01-baseline.md` (v1.2 era; pre-arbitrator wiring)
Config checksum: `107305372752333a68b3b2cf9bfabd44a1295172fb27dff3c4922fdbc217651a` (`tests/integration/test_phase4_e2e.py::_make_phase4_config`)
Artifact id (B-PROMO-04): `77cc345ad607eeb7ca41459e643c870ad0dd1d1c54e951fa396efd63edb49882` (`data_version="phase4-e2e-synth-v1"`; unchanged vs prior)
Tests: passed=**2041** failed=0 skipped=7 xfailed=0 (**vs prior baseline: +1** — additional bus-signal / arbitration coverage)
**Primary replay path:** not executed (`MASSIVE_API_KEY` + credentialed `run_backtest.py` unavailable in this run) — **MAJOR deferral** per v1.3 substitute clause.
**Substitute:** `pytest tests/integration/test_phase4_e2e.py -q` executed **twice** (8 passed @ ~3.5–3.9s each); exit 0 both runs.
Signal stream SHA-256 (fixture still vacuous for standalone signals): `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` — twin `_build()` match (**determinism OK**).
Footer `parity_hash (both)` from `run_backtest.py`: **N/A** (primary not run)
Stress 1.5x / 2.0x net PnL: **UNVERIFIED** (primary not run) — **MAJOR deferral** (same class as baseline)
tick_to_decision_latency_ns p50/p95/p99: ~57,400 / ~72,800 / ~176,000 ns (n=3600; Phase-4 synthetic scrape); **max ~6.90 ms** — under **10 ms** hard ceiling
backtest_validation !slow wall: **~122.4 s** (vs **~119.4 s** prior — **≈2.5%** increase, **<25%** A-PERFB-01 budget)
events/sec: same order of magnitude as baseline (~3k quotes/s synthetic)
**Iteration verdict: PASS** (Pass/Fail #1–6: no new BLOCKER vs prior; **B-MULTI-02 BLOCKER resolved**; MAJOR inventory not expanded; pytest clean; v1.3 substitute path for #4–5 documented; p99 OK)

## Pillar A — Structural

### A1. Layer separation & hidden state
- A-LAYER-01: PASS — Same spot methodology as baseline (`risk`↔`execution` forbidden edge: 0); no new violations observed.
- A-LAYER-02: PASS — No module-level mutable singleton pattern in sampled `risk/` layers.
- A-LAYER-03: FAIL MAJOR — Default `SignalPositionTranslator` / `BudgetBasedSizer` in `orchestrator.py` L333–337 when ctor deps omitted; bootstrap remains primary composer.

### A2. Event-bus & typed-schema discipline
- A-EVENT-01: PASS — Typed `Event` publishes; no dict payloads.
- A-EVENT-02: FAIL MAJOR — `EventBus.subscribe` still no runtime handler type enforcement (`event_bus.py` L37–43).
- A-EVENT-03: PASS — `Event` requires `timestamp_ns`, `correlation_id`, `sequence` without `None` defaults.

### A3. Clock abstraction
- A-CLOCK-01: FAIL MAJOR — `bootstrap.py` `time.time()`; `massive_ingestor.py` `time.monotonic()` (outside allowed exclusions per strict read).
- A-CLOCK-02: PASS — `SimulatedClock` monotonicity tests present.

### A4. State-machine integrity
- A-SM-01: PASS — Five frozen state machines present.
- A-SM-02: PASS — Enum completeness in `StateMachine.__init__`.
- A-SM-03: PASS — No `IllegalTransition` swallow outside tests.
- A-SM-04: PASS — `TransitionRecord` → `StateTransition` bus publish wired.
- A-SM-05: PASS — Risk monotonicity + audited unlock path (suite).

### A5. Backtest/live parity surface
- A-PARITY-01: PASS — Mode checks isolated to `bootstrap.py` / `platform_config.py`.
- A-PARITY-02: PASS — `_process_tick` mode-agnostic.
- A-PARITY-03: PASS — `_create_backend` BACKTEST table matches expectations.

### A6. Fail-safe defaults
- A-FAIL-01: PASS — `RiskAction` exhaustiveness (suite).
- A-FAIL-02: PASS — Ack unknown status handling.
- A-FAIL-03: PASS — Kill switch contract.
- A-FAIL-04: PASS — Horizon engine guards; no `composite.py`; PR-2b paths intact.

### A7. Determinism & provenance
- A-DET-01: PASS — No `uuid4` in execution/kernel hot path.
- A-DET-02: PASS (substitute + **MAJOR deferral** for primary footer hash) — Twin `_build()` + paired Phase-4 e2e runs; stream hash stable.
- A-DET-03: PASS — `snapshot().checksum` stable for unchanged config dict.
- A-DET-04: PASS — `run_backtest.py` global `exchange_timestamp_ns` sort (`_resequence`).

### A8. Performance measurement plumbing
- A-PERF-01: PASS — `tick_to_decision_latency_ns` `MetricEvent` at M10.
- A-PERF-02: PASS MINOR — Segment timers in `_process_tick`.
- A-PERF-03: PASS — No unguarded eager f-strings on hot path (spot).
- A-PERF-04: PASS — No asyncio/thread pool inside `_process_tick`.

### A8b. Performance budget enforcement
- A-PERFB-01: PASS — Wall **122.4s** vs **119.4s** prior (**<25%** regression).
- A-PERFB-02: PASS — p99 **0.176 ms** **≪ 10 ms** ceiling (substitute fixture scrape).
- A-PERFB-03: PASS MAJOR tracking — Events/s baseline carry-forward; <25% vs prior informal baseline.

### A9. Test-coverage spine
- A-TEST-01: PASS — One test area per major `src/feelies/` layer (non-empty).
- A-TEST-02: FAIL MAJOR — Hypothesis coverage not mapped 1:1 to all nine invariants (unchanged).
- A-TEST-03: PASS — `2041 passed, 7 skipped, exit 0` (`audits/_last_pytest_iter.txt`).

### A10. Data ingestion fidelity
- A-DATA-01: PASS — `NBBOQuote(` confined to ingestion + tests.
- A-DATA-02: PASS — Gap / recovery semantics in normalizer (suite).
- A-DATA-03: PASS — Dedup counters (`suite`).
- A-DATA-04: PASS — `correlation_id` boundary discipline (spot).
- A-DATA-05: PASS — EventLog append-only contract.
- A-DATA-06: FAIL MAJOR — `EventSerializer` protocol only; no durable log in `bootstrap` default graph.

### A11. Regime engine integration
- A-REGIME-01: PASS — Shared regime + hazard detector wiring (`bootstrap` + service tests).
- A-REGIME-02: PASS MAJOR — Writer/reader separation (spot).
- A-REGIME-03: PASS — Regime scalars in sizer (`position_sizer` tests).
- A-REGIME-04: FAIL MAJOR (permanent tracking) — Forensic divergence documentation gap.
- A-REGIME-05: FAIL MAJOR — No `regime_calibration.json`-style artifact in `alphas/**/*.alpha.yaml`.

### A12. Safety mechanisms triad
- A-SAFE-01: PASS — Kill switch.
- A-SAFE-02: FAIL (promotion BLOCKER) — Distinct circuit breaker not verified.
- A-SAFE-03: FAIL (promotion BLOCKER) — Capital throttle distinct from kill-switch not verified.
- A-SAFE-04: FAIL MAJOR — Triad bus typing incomplete for missing CB/throttle.
- A-SAFE-05: BLOCKER for promotion — Independent halt mechanisms incomplete.

### A13. Persistence durability
- A-PERSIST-01: PASS MAJOR context — In-memory defaults for BACKTEST acceptable.
- A-PERSIST-02: FAIL (promotion BLOCKER) — No durable backends for promotion modes.
- A-PERSIST-03: PASS — Determinism suite implies stable graph.
- A-PERSIST-04: FAIL MAJOR — No horizon warm-start checkpoint parity test found.

### A14. Secrets & credentials handling
- A-SEC-01: PASS — Env-based secrets expectation; no literals in `platform.yaml` spot.

### A15. Three-layer alpha architecture
- A-THREE-01: PASS — Loader + `PlatformConfig` gate flags.
- A-THREE-02: PASS — `SensorRegistry` bootstrap registration.
- A-THREE-03: PASS — Regime gate purity + compile safety.
- A-THREE-04: PASS — `CompositionEngine` Phase-4 wiring.

### A16. Promotion lifecycle integrity
- A-PROMO-01: PASS — Lifecycle matrix includes capital-tier self-loop.
- A-PROMO-02: PASS — Append-only ledger rollback semantics.
- A-PROMO-03: PASS — Gate matrix exhaustive at import.

## Pillar B — Causal Chain

### B1. Mechanism-before-trade
- B-MECH-01: PASS — All `alphas/**/*.alpha.yaml` carry `hypothesis` + `falsification_criteria`.
- B-MECH-02: PASS MAJOR — Falsification references measurable quantities.
- B-MECH-03: PASS — `risk_budget` present + consumed.
- B-MECH-04: PASS — Hypotheses name structural forces.

### B2. Causality / no-lookahead
- B-CAUSAL-01: PASS — Aggregator + sensor causal discipline (determinism suite).
- B-CAUSAL-02: PASS — `HorizonSignalEngine` guards; no legacy vector path.
- B-CAUSAL-03: PASS — Routers causal w.r.t. ack-time NBBO (tests).
- B-CAUSAL-04: PASS — Exchange-timestamp replay discipline.

### B3. Two-phase risk gate
- B-RISK-01: PASS — `check_sized_intent` per-leg + SIGNAL `check_order` path.
- B-RISK-02: PASS — `NO_ACTION` short-circuit.
- B-RISK-03: PASS — `SCALE_DOWN` re-check.
- B-RISK-04: PASS MAJOR — Skill table alignment operator-owned.

### B4. Position sizing & intent translation
- B-SIZE-01: PASS — Regime-aware sizer.
- B-SIZE-02: PASS — Per-symbol caps.
- B-SIZE-03: PASS — Translator matrix complete (tests).
- B-SIZE-04: FAIL MAJOR — `platform_min_order_shares` at multiple orchestrator sites (L1690, L1771, L1837, L1935).

### B5. Order-lifecycle & idempotency
- B-ORDER-01: PASS — Deterministic IDs + `_active_orders`.
- B-ORDER-02: PASS MAJOR — Ack exhaustiveness.
- B-ORDER-03: PASS MAJOR — Cancel semantics.
- B-ORDER-04: PASS MAJOR — Duplicate id drop in router tests.

### B6. Fill model realism
- B-FILL-01: PASS — `DefaultCostModelConfig` components.
- B-FILL-02: UNVERIFIED — Primary stress not run (**MAJOR deferral**).
- B-FILL-03: PASS MAJOR — Passive router semantics.
- B-FILL-04: PASS — Emergency MARKET exits.

### B7. Position & PnL reconciliation
- B-PNL-01: PASS — Fill → position + `TradeRecord` path.
- B-PNL-02: PASS MAJOR — `TradeRecord` computable fields.
- B-PNL-03: PASS MAJOR — Multi-alpha attribution via e2e + ledger tests (numeric parity not re-derived this run).
- B-PNL-04: PASS — MtM drawdown → escalation path.

### B8. End-to-end live-practice replay
- B-E2E-01: PASS (substitute scope) — Phase-4 e2e green; **MAJOR deferral** for footer metrics.
- B-E2E-02: PASS (substitute) — Determinism tests + twin stream hash; **not** `run_backtest.py` hash identity.
- B-E2E-03: UNVERIFIED — Stress (**MAJOR deferral**).
- B-E2E-04: FAIL MAJOR — `tests/test_backtest_e2e.py` still absent; partial coverage in `test_phase4_e2e.py` + determinism matrix; arbitration now additionally covered by `test_orchestrator_bus_signal.py`.
- B-E2E-05: PASS (promotion BLOCKER) — Paper/live stubs; `NotImplementedError` non-BACKTEST `_create_backend`.

### B9. Provenance & audit trail
- B-PROV-01: PASS — `TradeRecord` timestamps + `correlation_id`.
- B-PROV-02: PASS — Quote/trade logging path.
- B-PROV-03: PASS — Checksum in header.
- B-PROV-04: PASS MAJOR — Alpha versions + lifecycle.

### B10. Personal-trading guardrails
- B-GUARD-01: PASS — Stop-loss MARKET path.
- B-GUARD-02: PASS — Trailing tracker.
- B-GUARD-03: FAIL MAJOR — `signal_entry_cooldown_ticks` config-only (orphaned).
- B-GUARD-04: FAIL MAJOR — Multiple min-order enforcement sites (see B-SIZE-04).
- B-GUARD-05: PASS MAJOR — `account_equity` documentation.
- B-GUARD-06: FAIL MAJOR — `float` in risk/hazard paths vs Decimal money ideal.

### B11. Multi-alpha arbitration & attribution
- B-MULTI-01: PASS — `Signal` subscriber L462 + `SizedPositionIntent` subscriber (PR-2b-iii/iv).
- B-MULTI-02: **PASS** — `Orchestrator._select_bus_signal` calls `self._signal_arbitrator.arbitrate(buf)` (defaults `EdgeWeightedArbitrator`, ctor `signal_arbitrator` injectable) — `orchestrator.py` L2517–2543; tests `test_orchestrator_bus_signal.py` (`test_arbitration_prefers_higher_composite_over_first_arrival`).
- B-MULTI-03: PASS — `AlphaBudgetRiskWrapper` isolation.
- B-MULTI-04: PASS MAJOR — Attribution tests; primary two-alpha `run_backtest` sum not run.
- B-MULTI-05: FAIL MAJOR — Cross-strategy correlation budget doc gap in YAMLs.

### B12. Promotion-gate readiness
- B-PROMO-01: NO — Full checklist walk deferred.
- B-PROMO-02: NO — Paper backend not production-wired.
- B-PROMO-03: NO — No live small-cap window in-repo.
- B-PROMO-04: PASS — `artifact_id` stable (this iteration matches prior).

### B13. Decay-detection plumbing
- B-DECAY-01: PASS MAJOR — Forensics modules reachable.
- B-DECAY-02: PASS MAJOR — Crowding plumbing partial.
- B-DECAY-03: PASS MAJOR — Latency disadvantage from `TradeRecord` times.
- B-DECAY-04: FAIL MAJOR — Auto-quarantine wire partial.

### B14. Sensor-to-signal pipeline & hazard controls
- B-SENSOR-01: PASS — Sensor → aggregator → snapshot → signal engine path.
- B-HAZARD-01: PASS — `HazardExitController` bootstrap + bus MARKET exits.
- B-HAZARD-02: PASS — `RegimeHazardDetector` read-only spike emission.

## Promotion-gate readiness summary

- Research → Paper: **NO**
- Paper → Small Capital: **NO**
- Small → Scaled: **NO**

## Deltas vs prior audit (`2026-05-01-baseline.md`)

- **Resolved BLOCKER:** `B-MULTI-02` — `EdgeWeightedArbitrator` now default drain for multi-standalone `Signal` buffer (`orchestrator.py`); protocol bumped **v1.3** documents substitute replay harness.
- **Resolved META gap:** `AUDIT_PROTOCOL.md` step 4 / pass-fail #4–5 no longer reference retired `--demo`; aligns with `scripts/run_backtest.py` docstring.
- **New:** none (code promotion: +1 passing test; arbitration tests).
- **Regression watch:** A-PERFB-01 wall +2.5% (within 25% envelope).

## Risk register (permanent / known — re-listed)

- **Persistence** — in-memory default BACKTEST → **BLOCKER for promotion** to PAPER/LIVE.
- **Paper/live** — stubs only; `NotImplementedError` in `_create_backend` non-BACKTEST → **BLOCKER for promotion**.
- **Durable `EventLog`** — protocol only / no default disk DB → **MAJOR** until wired.
- **Sim-vs-live harness** → **BLOCKER for promotion** to LIVE.
- **Circuit breaker / capital throttle** (distinct from kill-switch) → **BLOCKER for promotion** (unverified).
- **Session calendar** → **MAJOR** (paper realism).
- **Global timestamp sort** — implementation PASS (`run_backtest.py`); register wording may lag.

## Meta (audit-of-the-audit) — `META-01`

- **Delta since prior report:** `audits/AUDIT_PROTOCOL.md` **v1.2→v1.3** (replay harness, `B-MULTI-02` criterion, table rows); `audits/README.md`, `_template.md`; `src/feelies/kernel/orchestrator.py` (arbitrator wiring); `tests/kernel/test_orchestrator_bus_signal.py` — **no new check IDs required** (existing **`B-MULTI-02`**, **`A-DET-02`**, **`B-E2E-*`** rows cover the protocol delta by design).
- **`.cursor/rules/platform-invariants.mdc` / `.cursor/skills/**`:** no edits attributed to this iteration’s evidence gathering.

## Action queue (carried forward)

1. ~~Reconcile B-MULTI-02 / protocol demo path~~ — **done** this cycle.
2. Wire or document **`signal_entry_cooldown_ticks`** (B-GUARD-03).
3. Consolidate **`platform_min_order_shares`** enforcement narrative or single-site refactor (B-SIZE-04 / B-GUARD-04).
4. Run credentialed **`run_backtest.py`** + stress 1.5/2.0; paste footer **`parity_hash (both)`** + PnL into next report.
5. Add **`tests/test_backtest_e2e.py`** or retitle protocol reference to existing integration/determinism matrix (B-E2E-04).

---

`verdict=PASS BLOCKER=0(new) MAJOR=deferral+known_inventory parity_hash_stable=Y(substitute+e2e×2) tests_delta=+1 promotion_ready=research`
