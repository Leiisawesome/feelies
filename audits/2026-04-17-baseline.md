# Audit 2026-04-17 — d05ac6859db55a82e8d59ad50cfa55ef284818b4

Protocol version: v1.0
Config checksum: drifts between runs (see A-DET-02/A-DET-03 — `--demo` snapshot includes a fresh `tempfile.mkdtemp()` path). Demo run 1 = `e49ddb8367edc939599aae940a172c86dfcd089b7ebc9c9a07b0402273d900d5`; demo run 2 = `ef7bb77ec32918e5c8601a55e2d75fee91c18f5506b90021cc09451fbf703b48`.
Artifact id: not yet computed (no canonical engine_version / data_version hash exists; recorded as `B-PROMO-04` finding below).
Tests: passed=1040 failed=0 skipped=3 xfailed=0 (vs prior: baseline)
Demo backtest parity hash: see below; **hashes are not bit-identical between runs**
Demo run twice — parity hashes:
  - run 1: `4b29751d112682efa25a2b3110815477c5ceafbd62dcbac00fdae64086b310e9`
  - run 2: `c00cab40ac3b7a9f10b67b24de586621f466dd5f9d5e47b39e720c50b09cf063`
  - bit-identical: **NO** (config_hash component drifts; pnl_hash component is bit-identical: `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` on both runs)
Stress 1.5x net PnL: $0.00 (0 trades; demo synthetic data does not produce signals — see `B-E2E-04`)
Stress 2.0x net PnL: $0.00 (0 trades; same)
tick_to_decision_latency_ns p50 / p95 / p99: ~41us avg / 89us / 89us (well under 10ms hard ceiling)
Iteration verdict: **FAIL**  (Pass/Fail criterion #4 fails: parity hash is not bit-identical across two back-to-back demo runs.)

## Pillar A — Structural

### A1. Layer separation & hidden state
- A-LAYER-01: PASS — every module under [src/feelies/](../src/feelies/) buckets into one of the documented layers; cross-layer imports are limited to bootstrap composition and the kernel orchestrating downstream layers via protocols.
- A-LAYER-02: PASS — grep for `^global ` and `_INSTANCE = ` / `_SINGLETON = ` in `kernel/`, `risk/`, `execution/`, `features/` returns zero hits.
- A-LAYER-03: PASS — `BasicRiskEngine`, `MemoryPositionStore`, `BacktestOrderRouter`, `PassiveLimitOrderRouter`, `BudgetBasedSizer`, `SignalPositionTranslator` are constructed only inside [src/feelies/bootstrap.py](../src/feelies/bootstrap.py) and `tests/`.

### A2. Event-bus & typed-schema discipline
- A-EVENT-01: PASS — 39 `bus.publish(...)` sites in the orchestrator all publish typed `Event` subclasses (`NBBOQuote`, `Signal`, `OrderRequest`, `OrderAck`, `PositionUpdate`, `RiskVerdict`, `Alert`, `MetricEvent`, `StateTransition`, `RegimeState`); no raw `dict`/`tuple` publishes found.
- A-EVENT-02: PASS — [src/feelies/bus/event_bus.py](../src/feelies/bus/event_bus.py) `subscribe(event_type: type[Event], handler)` routes by `type(event)` so subscribers only receive their declared type.
- A-EVENT-03: PASS — `Event` base class requires `correlation_id`, `timestamp_ns`, `sequence`; constructors of subclasses (`NBBOQuote`, `Signal`, `OrderRequest`, `OrderAck`, `Alert`, `TradeRecord`) all carry these fields end-to-end.

### A3. Clock abstraction
- A-CLOCK-01: PASS (with MINOR note) — only one `time.monotonic()` call outside `core/clock.py` and `monitoring/`: [src/feelies/ingestion/massive_ingestor.py:209,214,219](../src/feelies/ingestion/massive_ingestor.py) for live-paging progress callback (not on the replay/event-time path). Acceptable; recommend moving behind the clock abstraction. **MINOR**
- A-CLOCK-02: PASS — `SimulatedClock.set_time` enforces monotonicity (raises if rewinding) and is exercised by `tests/validation/test_determinism_props.py::test_simulated_clock_never_moves_backward`.

### A4. State-machine integrity
- A-SM-01: PASS — all five SM modules present: [kernel/macro.py](../src/feelies/kernel/macro.py), [kernel/micro.py](../src/feelies/kernel/micro.py), [execution/order_state.py](../src/feelies/execution/order_state.py), [risk/escalation.py](../src/feelies/risk/escalation.py), [ingestion/data_integrity.py](../src/feelies/ingestion/data_integrity.py).
- A-SM-02: PASS — `tests/validation/test_determinism_props.py::test_incomplete_*_table_raises` confirms SM construction triggers an enum-completeness check.
- A-SM-03: PASS — grep `except IllegalTransition` outside `tests/` returns zero hits; never swallowed.
- A-SM-04: PASS — orchestrator wires `_emit_state_transition` into every SM via `sm.on_transition(...)` (e.g. line 2239, 2556) so every transition publishes a `StateTransition` event.
- A-SM-05: PASS — `Orchestrator.unlock_from_lockdown(audit_token=...)` (line 494) requires explicit token; `KillSwitch.reset(operator, audit_token)` ([monitoring/kill_switch.py:50](../src/feelies/monitoring/kill_switch.py), [monitoring/in_memory.py:196](../src/feelies/monitoring/in_memory.py)) requires audit token.

### A5. Backtest/live parity surface
- A-PARITY-01: PASS — only allowed sites use mode discrimination: bootstrap (`config.mode == OperatingMode.BACKTEST` x3) and [core/platform_config.py](../src/feelies/core/platform_config.py) default. The single hit in [kernel/orchestrator.py:202](../src/feelies/kernel/orchestrator.py) is a docstring asserting the invariant.
- A-PARITY-02: PASS — `_process_tick` / `_process_tick_inner` contain no mode branch; `ExecutionBackend` is the only mode-discriminating surface.
- A-PARITY-03: PASS — `bootstrap._create_backend` composes `(SimulatedClock, InMemoryEventLog, BacktestOrderRouter|PassiveLimitOrderRouter)` for BACKTEST and explicitly raises `NotImplementedError` for non-BACKTEST modes (paper / live composition is the future-work contract).

### A6. Fail-safe defaults
- A-FAIL-01: PASS — `_process_tick_inner` has explicit branches for every `RiskAction` at M5 and M6 with a final `if action not in (...): raise ValueError(...)` (lines 870-874 and 947-952).
- A-FAIL-02: PASS — `_apply_ack_to_order` ends with an `else: raise ValueError("Unhandled OrderAckStatus: ...")` (line 2306-2310).
- A-FAIL-03: PASS — `KillSwitch.reset(*, operator, audit_token)` is the only re-arm path; absent the token there is no programmatic exit from lockdown.
- A-FAIL-04: PASS — [src/feelies/alpha/composite.py](../src/feelies/alpha/composite.py) `CompositeSignalEngine.evaluate` returns `None` when `not features.warm` (line 579) and on `features.stale` for entry signals (line 603).

### A7. Determinism & provenance
- A-DET-01: PASS — order IDs are SHA-256 of `correlation_id + sequence` (`hashlib.sha256(f"{cid}:{seq}".encode()).hexdigest()[:16]`, line 2051-2053); only `uuid4` mentions in the repo are inside docstrings forbidding it.
- A-DET-02: **FAIL — BLOCKER** — two back-to-back `python scripts/run_backtest.py --demo` runs produce different `parity_hash` because the `config_hash` component drifts. Root cause: [scripts/run_backtest.py:369-377](../scripts/run_backtest.py) creates `tempfile.mkdtemp(prefix="feelies_demo_")` and assigns it to `config.alpha_spec_dir`, which `PlatformConfig._to_dict()` includes verbatim in the snapshot serialised for `checksum`. The `pnl_hash` component IS bit-identical across runs (`4f53cda1...`), so the underlying replay determinism (Inv 5) is intact; the surface artefact is not.
- A-DET-03: **FAIL — MAJOR** — `PlatformConfig.snapshot()` mixes deterministic content (`_to_dict()` -> `checksum`) with a non-deterministic `timestamp_ns=time.time_ns()` ([core/platform_config.py:155](../src/feelies/core/platform_config.py)). The `checksum` field is in principle stable, but the `--demo` path violates that because of the tempdir leakage above. Round-trip stability cannot be claimed for `--demo` until that is fixed.
- A-DET-04: **FAIL — MAJOR** — multi-symbol global timestamp sort: [scripts/run_backtest.py](../scripts/run_backtest.py) does not globally sort events across symbols by `exchange_timestamp_ns` before resequencing in the multi-symbol path; per-symbol ordering is correct but cross-symbol replay ordering is not enforced. Carried into the risk register.

### A8. Performance measurement plumbing
- A-PERF-01: PASS — `tick_to_decision_latency_ns` is captured per tick via `t_wall_start = time.perf_counter_ns()` and emitted at M10 in `_finalize_tick`; demo verification reports avg/p95/p99 latencies.
- A-PERF-02: PASS — per-segment timers `feature_compute_ns`, `signal_evaluate_ns`, `risk_check_ns` recorded into `self._tick_timings` (lines 762-764, 796-798, 837-839).
- A-PERF-03: PASS — no `logger.debug(f"...")` in `_process_tick_inner` (only branch-conditional `logger.exception` / `logger.critical` outside the hot path).
- A-PERF-04: PASS — grep `asyncio`, `Thread(`, `concurrent.futures`, `multiprocessing` in [kernel/orchestrator.py](../src/feelies/kernel/orchestrator.py) returns zero hits; tick pipeline is single-threaded.

### A8b. Performance budget enforcement
- A-PERFB-01: PASS (baseline) — `pytest -q` total wall time 64.78s on 1040 tests; recorded as the regression baseline.
- A-PERFB-02: PASS — `--demo` reports `Avg tick-to-decision 0.041 ms`, `p95 0.089 ms`, `p99 0.089 ms`. Well under the 10ms hard ceiling and the 3ms target.
- A-PERFB-03: PASS (baseline) — events/sec on the 8-tick demo is too small to be representative; flagged for re-measurement when a multi-day backtest path lands. Recorded as baseline.

### A9. Test-coverage spine
- A-TEST-01: PASS — every src layer (`alpha/`, `bus/`, `core/`, `execution/`, `features/`, `forensics/`, `ingestion/`, `kernel/`, `monitoring/`, `portfolio/`, `risk/`, `services/`, `signals/`, `storage/`) has a sibling test directory under `tests/`.
- A-TEST-02: **MAJOR** — `tests/validation/test_determinism_props.py` covers 8 of 9 testing-validation property invariants (clock monotonicity, idempotent submission, SM validity over random walks, enum completeness, monotonic risk, position conservation, micro pipeline returns to M0, deterministic replay via checkpoint/restore). Missing: explicit *PnL decomposition* property test. Add `test_pnl_decomposition_invariant` to close the gap.
- A-TEST-03: PASS — `pytest -q` exit code 0; `1040 passed, 3 skipped` (skip context not yet recorded; capture in next iteration). Baseline.

### A10. Data ingestion fidelity
- A-DATA-01: PASS — `NBBOQuote(` / `Trade(` are constructed only in [src/feelies/ingestion/massive_normalizer.py](../src/feelies/ingestion/massive_normalizer.py) and the definition site [src/feelies/core/events.py](../src/feelies/core/events.py); no other ingestion entry point.
- A-DATA-02: PASS — `MassiveNormalizer._check_gap` ([ingestion/massive_normalizer.py:367](../src/feelies/ingestion/massive_normalizer.py)) flips `DataHealth -> GAP_DETECTED` and the recovery branch (line 389) restores `HEALTHY` when `seq_num == prev_seq + 1`.
- A-DATA-03: PASS — `_duplicates_filtered` counter increments on duplicate (line 363); exposed via `IngestResult.duplicates_filtered` ([massive_ingestor.py:53,189](../src/feelies/ingestion/massive_ingestor.py)).
- A-DATA-04: PASS — `make_correlation_id` defined in [core/identifiers.py:8](../src/feelies/core/identifiers.py) and called only from `MassiveNormalizer` (4 sites at the ingestion boundary); no downstream re-assignment.
- A-DATA-05: PASS — [storage/event_log.py](../src/feelies/storage/event_log.py) Protocol exposes only `append` / `append_batch`; [memory_event_log.py](../src/feelies/storage/memory_event_log.py) implements only those two. No `update`/`delete`/`__setitem__`.
- A-DATA-06: PASS (with MAJOR caveat) — `EventSerializer` Protocol exists at [src/feelies/core/serialization.py:27](../src/feelies/core/serialization.py) (defines `serialize` / `deserialize` with bit-determinism contract) but **no concrete implementation is wired into bootstrap**. `DiskEventCache` exists at [storage/disk_event_cache.py:92](../src/feelies/storage/disk_event_cache.py) but is not used by `build_platform`. Updated risk register: protocol present, concrete + wiring required for durable replay. **MAJOR (downgraded from BLOCKER since the protocol now exists).**

### A11. Regime engine integration
- A-REGIME-01: PASS — bootstrap composes a single `regime_engine = _create_regime_engine(config.regime_engine)` and threads the same instance into `BasicRiskEngine`, `BudgetBasedSizer`, `AlphaLoader` (and via loader into signal engines that opt in). Demo runs with `regime_engine=None` so the path is exercised but not tested under load.
- A-REGIME-02: PASS — `RegimeState` is published only by [src/feelies/services/regime_engine.py](../src/feelies/services/regime_engine.py) implementations; consumers read via the engine instance. Grep shows no external mutation.
- A-REGIME-03: PASS — [src/feelies/risk/position_sizer.py:56,64](../src/feelies/risk/position_sizer.py) maps `vol_breakout -> 0.5x` (and the rest of the regime scalar table per skill).
- A-REGIME-04: **MAJOR** (permanent until forensics labels are online) — conservative-divergence rule (more conservative wins when forensic vs risk-state regime labels disagree) is documented in the regime-detection skill but no code path enforces it because the forensic regime classifier is not yet integrated. Carried in risk register.
- A-REGIME-05: **MAJOR** — `alphas/trade_cluster_drift/trade_cluster_drift.alpha.yaml` does not reference a versioned `regime_calibration.json`; the only versioned artefacts are the alpha and feature versions. Add a calibration-artifact reference once the regime engine is exercised in production.

### A12. Safety mechanisms triad
- A-SAFE-01: PASS — `KillSwitch` present (`InMemoryKillSwitch` at [monitoring/in_memory.py:163-207](../src/feelies/monitoring/in_memory.py)), irreversible without `reset(operator, audit_token)`; orchestrator gates the tick at [orchestrator.py:697-705](../src/feelies/kernel/orchestrator.py).
- A-SAFE-02: **FAIL — BLOCKER for promotion** — no class named `CircuitBreaker` (or equivalent cancel-only-no-flatten mechanism) exists in `src/`. Only the skill files reference it.
- A-SAFE-03: **FAIL — BLOCKER for promotion** — no `CapitalThrottle` (dynamic sizing scalar driven by health signals as a separate component from the kill switch) exists in `src/`. Only the skill files reference it.
- A-SAFE-04: PARTIAL — `KillSwitchActivation` typed event exists; circuit-breaker / capital-throttle equivalents cannot exist until the mechanisms do. Tracked together with A-SAFE-02/03.
- A-SAFE-05: N/A pending A-SAFE-02/03 — only one mechanism currently exists, so independent-halt invariant is trivially satisfied.

### A13. Persistence durability
- A-PERSIST-01: PASS (informational) — bootstrap inventory: `InMemoryEventLog`, `InMemoryTradeJournal`, `InMemoryFeatureSnapshotStore`, `InMemoryKillSwitch`, `InMemoryAlertManager`, `InMemoryMetricCollector`. `DiskEventCache` exists but is not wired.
- A-PERSIST-02: **BLOCKER for promotion** (permanent) — durable backends required before mode switch to `PAPER` / `LIVE`.
- A-PERSIST-03: PASS — `build_platform(config)` is pure with respect to `config`: re-invocation produces an equivalent component graph (verified structurally; the only wall-clock surface is `ConfigSnapshot.timestamp_ns` which does not enter the checksum). Reinforce by adding a smoke test next iteration.
- A-PERSIST-04: **MAJOR** — feature-snapshot warm-start round-trip is covered by `tests/validation/test_determinism_props.py::test_checkpoint_restore_produces_identical_output` for alpha lifecycle, but there is no equivalent test for `InMemoryFeatureSnapshotStore` checkpoint -> restore -> next-event determinism. Add `tests/storage/test_feature_snapshot_warm_start.py`.

### A14. Secrets & credentials handling
- A-SEC-01: PASS — grep of `MASSIVE_API_KEY`, `BROKER_`, `secret`, `token`, `password`, `Bearer ` under [src/](../src/), [scripts/](../scripts/), [alphas/](../alphas/), [platform.yaml](../platform.yaml) finds only parameter names (`api_key: str`, `audit_token: str`, `_RESTORE_TOKEN: object`) — no literal credential values. `platform.yaml` is credential-free.

## Pillar B — Causal Chain

### B1. Mechanism-before-trade
- B-MECH-01: PASS — `alphas/trade_cluster_drift/trade_cluster_drift.alpha.yaml` and `alphas/_template/template.alpha.yaml` both declare `hypothesis:` and `falsification_criteria:`.
- B-MECH-02: **MAJOR** — `falsification_criteria` are present but should be re-checked next iteration to confirm each entry references a metric the forensics layer can compute (currently a mix of measurable and narrative entries; full per-entry audit deferred to first multi-alpha iteration).
- B-MECH-03: PASS — both YAMLs include a `risk_budget:` block; `AlphaBudgetRiskWrapper` consumes them ([src/feelies/alpha/risk_wrapper.py](../src/feelies/alpha/risk_wrapper.py)).
- B-MECH-04: PASS — `hypothesis:` text in `trade_cluster_drift` describes a structural microstructure mechanism (cluster drift), not a curve-fit narrative.

### B2. Causality / no-lookahead
- B-CAUSAL-01: PASS — feature `update(quote, state)` signature in [src/feelies/features/library.py](../src/feelies/features/library.py) and [features/definition.py:62](../src/feelies/features/definition.py) takes only `(quote, state)`; no global lookahead inputs.
- B-CAUSAL-02: PASS — `CompositeSignalEngine.evaluate` suppresses on `not features.warm` and `features.stale` ([alpha/composite.py:579,603](../src/feelies/alpha/composite.py)).
- B-CAUSAL-03: PASS — `BacktestOrderRouter` and `PassiveLimitOrderRouter` evaluate fills on `on_quote(quote)` (current quote at ack time), not the signal-time snapshot.
- B-CAUSAL-04: PASS — grep for `Clock.now` / `clock.now` under `ingestion/`, `bus/`, `kernel/` outside monitoring/metric paths returns zero hits in `EventLog` write path. The orchestrator's `self._clock.now_ns()` calls stamp Alert / OrderRequest events; under `SimulatedClock` these remain replay-deterministic.

### B3. Two-phase risk gate
- B-RISK-01: PASS — `_process_tick_inner` calls `risk_engine.check_signal` at M5 (line 838) and `risk_engine.check_order` at M6 (line 903); no order-construction path skips either.
- B-RISK-02: PASS — `if intent.intent == TradingIntent.NO_ACTION` skips to `_finalize_tick` (line 822-829).
- B-RISK-03: PASS — `RiskAction.SCALE_DOWN` re-checks via `check_order` after applying `verdict.scaling_factor` (line 931-952), and falls through the M6 exhaustiveness guard.
- B-RISK-04: PASS — drawdown gate threshold matches `risk_max_drawdown_pct` from `RiskConfig` ([basic_risk.py:40,322](../src/feelies/risk/basic_risk.py)). The full per-tier table (warning / throttle / circuit / kill) is **not yet implemented as separate tiers** — currently a single `FORCE_FLATTEN` threshold is used. Documented divergence; tracked in risk register together with A-SAFE-02/03.

### B4. Position sizing & intent translation
- B-SIZE-01: PASS — `BudgetBasedSizer` consumes `regime_engine` (passed in ctor in `bootstrap.py:139`) and applies regime scalar.
- B-SIZE-02: PASS — sizer caps via `risk_budget.max_position_per_symbol` (alpha manifest) and the platform `risk_max_position_per_symbol`.
- B-SIZE-03: PASS — `_side_from_intent` covers ENTRY_LONG, ENTRY_SHORT, EXIT, SCALE_UP, REVERSE_LONG_TO_SHORT, REVERSE_SHORT_TO_LONG, and raises `ValueError` for unknown ([orchestrator.py:2125-2148](../src/feelies/kernel/orchestrator.py)). `SignalPositionTranslator.translate` covers the (SignalDirection x Position) matrix.
- B-SIZE-04: PASS — `_min_order_shares` enforced at exactly two sites: in `_compute_target_quantity` it is *clamped up* to avoid the dead-zone (line 1820-1821), and in `_build_order_from_intent` it is enforced as a *suppression gate* after sizing and before `OrderRequest` construction (line 2065-2066). Both are after sizing and before routing.

### B5. Order-lifecycle & idempotency
- B-ORDER-01: PASS — every `OrderRequest` carries an SHA-256 `order_id` (line 2051-2053) and is added to `_active_orders` via `_track_order` (line 2236-2240) until terminal.
- B-ORDER-02: PASS — `_apply_ack_to_order` is exhaustive over `OrderAckStatus` (REJECTED, ACKNOWLEDGED, FILLED, PARTIALLY_FILLED, CANCELLED, EXPIRED) with auto-ACK of `SUBMITTED` on early `FILLED` (line 2286-2287) and `_emit_ack_drop_alert` on inapplicable transitions.
- B-ORDER-03: PASS — `cancel_order` only transitions `ACKNOWLEDGED -> CANCEL_REQUESTED` and returns `False` if `can_transition` rejects (line 2169-2170); `PARTIALLY_FILLED` cannot cancel per `OrderState` SM definition.
- B-ORDER-04: PARTIAL — duplicate `order_id` on the router boundary is implicitly handled because order IDs are deterministic (replay produces the same ID, and the router's order book sees it as the same entry). No explicit drop-on-duplicate guard at the router boundary. **MINOR**.

### B6. Fill model realism
- B-FILL-01: PASS — `DefaultCostModelConfig` ([execution/cost_model.py:52](../src/feelies/execution/cost_model.py)) carries `min_spread_cost_bps`, `commission_per_share`, `taker_exchange_per_share`, `maker_exchange_per_share`, `passive_adverse_selection_bps`, `sell_regulatory_bps`, `stress_multiplier`, `min_commission`, `max_commission_pct`, `htb_borrow_annual_bps`. All cost components present.
- B-FILL-02: PASS (operationally) — `python scripts/run_backtest.py --demo --stress-cost 1.5` runs to completion. Net PnL = $0 (no trades on 8-tick synthetic data) so the `>=0` criterion is trivially satisfied; the cost-stress path is exercised end-to-end and parity_hash differs from the unstressed run as expected.
- B-FILL-03: PASS (structurally) — `PassiveLimitOrderRouter` ([execution/passive_limit_router.py](../src/feelies/execution/passive_limit_router.py)) implements through-fill and level-fill triggers and cancels after `passive_max_resting_ticks` (verified by grep + bootstrap parameter wiring).
- B-FILL-04: PASS — stop-exit branch in `_build_order_from_intent` (line 2104-2108) explicitly *skips* the LIMIT path and uses `OrderType.MARKET`, even when `_use_passive_entries` is on. Inv 11 fail-safe upheld.

### B7. Position & PnL reconciliation
- B-PNL-01: PASS — `_reconcile_fills` (line 2334) updates `MemoryPositionStore.update(...)` atomically and emits `PositionUpdate` + writes a `TradeRecord` (line 2435-2464).
- B-PNL-02: PASS — `TradeRecord` carries `requested_quantity`, `filled_quantity`, `fill_price`, `cost_bps`, `fees`, `realized_pnl`; combined with the position store, gross / realized / unrealized / fees / slippage are computable per trade.
- B-PNL-03: PARTIAL — `FillAttributionLedger` and `StrategyPositionStore` are wired and populated in `_reconcile_fills` (line 2398-2434); the proportional-distribution fallback is correct. A fully reconciled sum check on a multi-alpha demo run is pending until a second alpha is registered. **MAJOR (test gap, not a code defect).**
- B-PNL-04: PASS — per-tick mark-to-market: `mid = (quote.bid + quote.ask) / 2` is pushed into both position stores (line 734-738) every tick; `BasicRiskEngine._check_max_drawdown` (line 322) gates trades and triggers `FORCE_FLATTEN -> _escalate_risk(cid)` in the orchestrator (line 843-850 / 906-913).

### B8. End-to-end live-practice replay
- B-E2E-01: PASS — `python scripts/run_backtest.py --demo` ran cleanly. Captured: trade_count=0, gross/net PnL=$0, max DD=$0, kill state=INACTIVE, parity_hash=`4b29751d...`. (exit code 2 because the demo's internal verification expects signals/fills, which the 8-tick synthetic data doesn't trigger; this is informational, not a run failure.)
- B-E2E-02: **FAIL — BLOCKER** — see A-DET-02. Two back-to-back demo runs produce different `parity_hash` (config-snapshot tempdir leakage). `pnl_hash` is bit-identical, so the *replay-determinism* invariant (Inv 5) holds for trade outputs; the *surface artefact* contract does not.
- B-E2E-03: PASS — both `--stress-cost 1.5` and `--stress-cost 2.0` runs completed; net PnL = $0 (no trades on the demo); the stress-cost path is exercised. Once the demo is augmented to actually fill, this check has teeth.
- B-E2E-04: **MAJOR (multiple gaps)** — `tests/test_backtest_e2e.py` exists; the protocol's required scenarios still need explicit coverage stubs:
  - warm-up phase: stub `test_e2e_warmup_suppresses_signals` (verify no signals before warm threshold).
  - regime transitions: stub `test_e2e_regime_transition_scales_size` (vol_breakout x 0.5).
  - circuit-breaker firing: blocked on A-SAFE-02 (no breaker yet).
  - reconnect / gap recovery: stub `test_e2e_gap_recovery_health_transition`.
  - multi-alpha arbitration: stub `test_e2e_multi_alpha_arbitration_deterministic`.
- B-E2E-05: **BLOCKER for promotion** (permanent) — sim-vs-live divergence harness absent (paper / live router are stubs raising `NotImplementedError`). Carried in risk register; cannot run drift tests until the live router exists.

### B9. Provenance & audit trail
- B-PROV-01: PASS — every `TradeRecord` carries `correlation_id`, `signal_timestamp_ns`, `submit_timestamp_ns`, `fill_timestamp_ns` (orchestrator.py:2449-2464). Today `signal_timestamp_ns` is set to the order timestamp (a known minor fidelity gap — true signal-time stamping requires propagating the originating quote timestamp through `OrderIntent`). **MINOR**.
- B-PROV-02: PASS — `event_log.append(quote)` at M1 (line 726) for every tick; `_process_trade` (verified by grep) appends each trade.
- B-PROV-03: PASS — `PlatformConfig.snapshot().checksum` recorded in this report. Future iterations diff this field; current limitation noted under A-DET-02/03.
- B-PROV-04: PASS — both alpha YAMLs declare `version` + `schema_version`. `AlphaRegistry` records `lifecycle` per alpha (RESEARCH today).

### B10. Personal-trading guardrails
- B-GUARD-01: PASS — `_check_stop_exit` evaluates `stop_loss_per_share` per tick from last NBBO mid and emits a synthetic `__stop_exit__` `Signal` (line 1740-1783); the order construction path forces `OrderType.MARKET` for stop exits and bypasses passive cooldowns / min-order-share gate (line 2061-2066, 2104-2108). Inv 11 upheld.
- B-GUARD-02: PASS — peak tracker `self._peak_pnl_per_share[symbol]` is per-symbol, monotonic (`if unrealized_per_share > peak: peak = unrealized_per_share`), and reset on flat (`pop` at line 1745, 2396).
- B-GUARD-03: PASS — `signal_entry_cooldown_ticks` is consumed by `CompositeSignalEngine` ([alpha/composite.py:91-93](../src/feelies/alpha/composite.py)) which keys cooldowns per `(symbol, strategy)` and resets on EXIT.
- B-GUARD-04: PASS — see B-SIZE-04. Single suppression site at `_build_order_from_intent` line 2065-2066 (after sizing, before `OrderRequest` construction, before routing).
- B-GUARD-05: **MAJOR** — `_account_equity` is initialised from `config.account_equity` (start-of-day) and is **not** rebased to live mark-to-market equity tick-to-tick. The risk-budget assumption in alpha YAMLs is unclear; document the chosen semantics in `platform.yaml` and the alpha schema, and add a regression test.
- B-GUARD-06: **MAJOR** — money-typed fields (price, fill_price, fees, cumulative_fees, realized_pnl) are `Decimal` in `Position`, `TradeRecord`, `PositionUpdate`, `OrderRequest`, `OrderAck`, and `DefaultCostModelConfig`. However, `risk/basic_risk.py` and `risk/position_sizer.py` use `float` for percentage / scalar fields (drawdown_pct, regime scalars). Recommend: keep money in `Decimal`, percentages in `Decimal` too at the boundary of risk decisions; spot-check by adding a property test that the verdict is identical between Decimal and float inputs at the precision boundary.

### B11. Multi-alpha arbitration & attribution
- B-MULTI-01: PASS — `MultiAlphaEvaluator` is wired only when `len(registry) > 1` ([bootstrap.py:156-165](../src/feelies/bootstrap.py)).
- B-MULTI-02: PARTIAL — `SignalArbitrator` is a Protocol ([alpha/arbitration.py:17](../src/feelies/alpha/arbitration.py)). Concrete deterministic implementation needs to be confirmed wired and covered by a property test. **MAJOR**.
- B-MULTI-03: PASS — `AlphaBudgetRiskWrapper` ([alpha/risk_wrapper.py](../src/feelies/alpha/risk_wrapper.py)) gates per-alpha drawdown via `risk_budget.max_drawdown_pct`; isolation enforced.
- B-MULTI-04: **MAJOR** — `FillAttributionLedger` totals reconciliation against `MemoryPositionStore` is not exercised by an automated multi-alpha demo. Add `test_multi_alpha_fill_attribution_reconciles` once a second alpha is registered.
- B-MULTI-05: **MAJOR** (permanent) — cross-strategy correlation budget is not documented in any alpha YAML. Add a `correlation_budget` block to the `alphas/_template/template.alpha.yaml` schema.

### B12. Promotion-gate readiness
- B-PROMO-01 (Research -> Paper): **NO** — pytest passes (1040/1043) and replay-determinism on the **trade** dimension (`pnl_hash`) is bit-identical, but the **parity-hash** surface contract fails (A-DET-02). Fault-injection coverage is partial. Fix A-DET-02 + close A-TEST-02 PnL-decomposition gap before reconsidering.
- B-PROMO-02 (Paper -> Small Capital): **NO** — paper router is a stub; sim-vs-live divergence harness absent (B-E2E-05).
- B-PROMO-03 (Small -> Scaled): **NO** — depends on >=10 trading days of small-capital live data; not applicable until B-PROMO-02 resolves.
- B-PROMO-04: **FAIL — MAJOR** — `hash(strategy_version, config_version, data_version, engine_version)` is not currently computed by `scripts/run_backtest.py`. Add as a derived field of the `--demo` report so consecutive audits can compare.

### B13. Decay-detection plumbing
- B-DECAY-01: PASS (partial) — [src/feelies/forensics/decay_detector.py](../src/feelies/forensics/decay_detector.py) `DecayDetector.analyze_fills(trades)` computes mean / p95 cost-bps, edge-bps, rolling 50/200 means, edge-vs-cost coverage. Reachable from `TradeJournal.query()` data. Per-skill labels (slippage residual, hit-rate residual, fill-rate drift) are not separately implemented — covered functionally but not by name. **MAJOR (naming + coverage gap).**
- B-DECAY-02: **FAIL — MAJOR** — no crowding scorecard (3+ symptom rule) implemented; grep returns no `crowding` references in `src/`.
- B-DECAY-03: **FAIL — MAJOR** — no latency-disadvantage signal computed from `signal_ts` / `submit_ts` / `fill_ts` in the forensics module. `TradeRecord` carries the timestamps but nothing aggregates them.
- B-DECAY-04: **FAIL — MAJOR** — no quarantine pathway from forensic finding -> `Alert` event -> risk scaling reduction is wired. `DecayDetector.detect_edge_decay` produces a `DecaySignal` data class but it is not consumed by any risk component.

## Promotion-gate readiness summary

- Research -> Paper: **NO**  (blocking: A-DET-02 BLOCKER, A-TEST-02 MAJOR, B-DECAY-02/03/04 MAJOR, B-PROMO-04 MAJOR)
- Paper -> Small Capital: **NO**  (blocking: A-PERSIST-02, A-SAFE-02, A-SAFE-03, B-E2E-05; all permanent until durable backends + safety triad + paper router + divergence harness exist)
- Small -> Scaled: **NO**  (downstream of Paper -> Small Capital)

## Deltas vs prior audit

This is the **baseline** report. There is no prior audit, so all findings count as the initial state and not as deltas. Future iterations diff against this file.

- New BLOCKER: A-DET-02 (parity hash drift via `--demo` tempdir leakage)
- Resolved: n/a (baseline)
- New MAJOR: A-CLOCK-01 (informational MINOR), A-DET-03, A-DET-04, A-TEST-02, A-DATA-06, A-REGIME-04, A-REGIME-05, A-PERSIST-04, B-MECH-02, B-PNL-03, B-GUARD-05, B-GUARD-06, B-MULTI-02, B-MULTI-04, B-MULTI-05, B-DECAY-01, B-DECAY-02, B-DECAY-03, B-DECAY-04, B-PROMO-04, B-E2E-04
- Resolved MAJOR: n/a (baseline)

## Risk register (permanent / known findings — re-listed every iteration)

- **Persistence**: storage backends are in-memory only (`InMemoryEventLog`, `InMemoryTradeJournal`, `InMemoryFeatureSnapshotStore`, `InMemoryKillSwitch`, `InMemoryAlertManager`, `InMemoryMetricCollector`). `DiskEventCache` exists ([storage/disk_event_cache.py](../src/feelies/storage/disk_event_cache.py)) but is not wired into `build_platform`. **BLOCKER for promotion** to PAPER/LIVE.
- **No paper / live router**: `_create_backend` raises `NotImplementedError` for non-BACKTEST modes; `PaperOrderRouter` and `LiveOrderRouter` are stub classes whose `__init__` raises. **BLOCKER for promotion** to PAPER/LIVE.
- **No durable `EventSerializer` implementation**: `EventSerializer` Protocol exists ([src/feelies/core/serialization.py](../src/feelies/core/serialization.py)) but no concrete implementation is exposed; without one, `DiskEventCache` cannot enforce bit-deterministic round-trip. **MAJOR** until persisted replay is testable.
- **Sim-vs-live divergence harness absent**: cannot run drift tests until live router exists. **BLOCKER for promotion** to LIVE.
- **Circuit breaker absent**: no class distinct from `KillSwitch` that cancels open orders without flattening exists. **BLOCKER for promotion** to PAPER/LIVE.
- **Capital throttle absent**: no class distinct from `KillSwitch` that scales sizing across health signals exists. **BLOCKER for promotion** to PAPER/LIVE.
- **Session calendar absent**: no NYSE/NASDAQ market-hours awareness in the code path. **MAJOR** for paper-trading realism.
- **Multi-symbol global timestamp sort**: `scripts/run_backtest.py` does not enforce global sort across symbols by `exchange_timestamp_ns` before resequencing. **MAJOR** for any multi-symbol run.
- **Conservative-divergence rule (regime)**: when forensic vs risk-state regime labels disagree, more conservative wins — not enforced because forensic regime classifier is not integrated. **MAJOR** until forensics regime is online.
- **Demo tempdir leakage in config snapshot**: `--demo` writes a fresh `tempfile.mkdtemp()` into `config.alpha_spec_dir`, which enters `PlatformConfig._to_dict()` and breaks `parity_hash` stability across runs. **BLOCKER** until snapshot normalises path-based fields or the demo writes its alphas to a fixed test-only path.
- **Cross-strategy correlation budget absent**: no alpha YAML declares it; `alphas/_template/template.alpha.yaml` lacks the field. **MAJOR**.

## Meta (audit-of-the-audit)

- META-01: PASS (baseline) — protocol `v1.0` is the lock point. No prior `.cursor/rules/platform-invariants.mdc` or `.cursor/skills/**` deltas to evaluate. Future iterations must list any change to those files since `2026-04-17` and either add a new check ID or justify why no new check is required. The next iteration's META-01 must explicitly note that the protocol's "no paper / live router" risk-register entry was refined here (paper/live router *files* exist as stubs, not absent).

## Action queue (carried to next iteration)

1. **Fix A-DET-02 (BLOCKER)**: normalise `config.alpha_spec_dir` in `--demo` (use a stable in-repo test path) or exclude path-based fields from `PlatformConfig._to_dict()`'s serialised form before checksumming.
2. **Add the missing PnL-decomposition property test (A-TEST-02 MAJOR)**: `tests/validation/test_pnl_decomposition_invariant.py`.
3. **Add explicit forensics implementations (B-DECAY-02/03/04)**: crowding scorecard, latency-disadvantage signal, quarantine pathway from `DecaySignal -> Alert -> risk scaling`.
4. **Add `B-PROMO-04` artifact-id computation** to `scripts/run_backtest.py` so consecutive audits can compare `hash(strategy_version, config_version, data_version, engine_version)`.
5. **Wire `EventSerializer` concrete implementation** to enable `DiskEventCache` and clear A-DATA-06 / A-PERSIST.
6. **Decide and document `account_equity` semantics** (start-of-day NAV vs live MtM) for B-GUARD-05.
7. **Augment the `--demo` data set** so that 1-2 actual fills occur, giving B-FILL-02 and B-E2E-03 real teeth.
8. **Add e2e test stubs** listed under B-E2E-04 (warm-up, regime transition, circuit-breaker, gap recovery, multi-alpha arbitration).
9. **Add a `correlation_budget:` block** to `alphas/_template/template.alpha.yaml` (B-MULTI-05).
10. **Investigate `risk/basic_risk.py` float annotations** (B-GUARD-06) — confirm percent fields can stay `float` or migrate to `Decimal`.
